legacy-peer-deps=true
# DEP-15 / SCRUM-1005: pin every install to an exact version when run
# from this directory. Mirrors the root .npmrc — npm does not inherit
# parent .npmrc as a project config, so each package needs its own.
save-exact=true
