/**
 * Deliberately empty compatibility module.
 *
 * Ceremony chronology is implemented by a module-private fixed state machine
 * inside `s33-batch-acceptance.ts`. Nothing here may expose a generic ledger,
 * append operation, or caller-provided event validator.
 */

export {};
