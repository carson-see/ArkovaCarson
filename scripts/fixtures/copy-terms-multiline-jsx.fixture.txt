// Fixture for scripts/check-copy-terms.test.ts — DO NOT "fix" the banned terms here.
// Reproduces the exact multi-line-JSX blind spot that let banned §1.3 chain terms
// ship to prod in src/components/verification/PublicVerification.tsx:658 (removed by
// PR #1433; scanner fixed alongside this fixture). The banned terms sit on raw JSX
// text lines that contain neither a quote character nor a same-line `<`/`>` pair, so
// the pre-fix per-line short-circuit in findTermViolations() never scanned them.
// (This header deliberately avoids the banned phrase — the test locates it by line.)
//
// Stored as .txt (TSX source read by the test) so repo typecheck/eslint never parse it.
export function VerificationDisclaimer() {
  return (
    <div className="rounded-lg bg-muted/50 border border-border p-4 space-y-2">
      <p className="text-xs font-medium text-muted-foreground uppercase tracking-wider">
        Verification Disclaimer
      </p>
      <p className="text-xs text-muted-foreground leading-relaxed">
        Arkova verifies that a document&apos;s fingerprint was anchored to the
        Bitcoin blockchain at the stated time. Arkova does not verify, and makes no
        representation regarding, the accuracy, authenticity, or legitimacy of the underlying
        document content. Relying parties should exercise their own due diligence.
      </p>
    </div>
  );
}
