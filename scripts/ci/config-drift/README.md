# config↔reality drift + cross-runtime parity gate (S0-5.2 SPIKE)

Lane-1 Sprint-0 spike. **Scaffolds the mechanism to retire R-5** (a deploy-config change silently breaks a core feature — the env↔DB fail-open class from 2026-05-30, and the mempool.space-provider SPOF class). The live source-parse + `/health` capture that actually *retires* R-5 lands in Sprint 1 (see Limitations) — don't mistake this spike for the mitigation.

## What it does

`scripts/ci/check-config-drift.ts` diffs **asserted** config against a **running** snapshot, failing CLOSED on drift across **flags / provider / fee-strategy / CSP** (a flag *enabled in running but unpinned in asserted* is caught too), then runs the **cross-runtime parity** harness (`lib/runtimeParity.ts`) over the running worker vs edge.

- **Asserted** = `expected-prod-config.json` — the intended **effective** prod config. For kill-switch flags the effective value is the **DB (`switchboard_flags`) resolution, not the raw deploy env var** — e.g. `ENABLE_SEMANTIC_SEARCH` is env=`true` but DB=`false` (fails closed) ⇒ effective `false`. The CSP list is the worker+edge **subset** of `vercel.json` connect-src.
- **Running** = `prod-config-snapshot.json` — a **reference snapshot of intended effective state** (not a live capture). Schema-validated on load → fails closed on a degraded file.

Run locally: `npx tsx scripts/ci/check-config-drift.ts` (exit 0 = aligned; exit 1 + `::error::` = drift). Tested by `scripts/ci/check-config-drift.test.ts` + `scripts/ci/lib/runtimeParity.test.ts` (inject synthetic drift → confirm catch).

## SPIKE boundary → Sprint 1

This proves the **mechanism**. Sprint 1 (S0-E5 → VIS-01 / CHAIN-RESIL) hardens it:
1. `loadAssertedConfig` **parses** config.ts / deploy-worker.yml / vercel.json directly (no hand-maintained JSON).
2. `loadRunningSnapshot` becomes a **read-only cron** capture of `GET /health` + the flag registry (never a write; never embeds secrets; PII-scrubbed).
3. The parity harness grows the marked extension points (route contracts, per-route auth mode, rate-limit headers, embedding-model parity).
4. Thresholds shared with the VIS-01 dashboard (see `docs/sprint-0/lane1/visibility-signal-inventory.md`).
5. ✅ **env↔DB flag fail-open SPOF — DONE (S1, Lane-2).** `config-drift/flagSpof.ts` parses the REAL `deploy-worker.yml` `--set-env-vars` flags + the REAL `flagRegistry.ts` `DB_FLAGS` list and classifies the env↔DB delta against the asserted `flags`. `flagRegistry` resolves a DB-backed flag to its env var when the `switchboard_flags` row is **absent**, so a flag asserted effective=`false` but env=`true` fails **OPEN** (the 2026-05-30 class). Findings: `fail-open-flag` (DB-backed, asserted-OFF/env-ON — OFF rests on a DB row), `env-flag-on-no-db-guard` (asserted-OFF/env-ON but NOT DB-backed — no kill switch at all), `launch-flag-off` (a `launchRequiredFlags` flag the deploy sets false/omits — launch-path break). Wired into `check-config-drift.ts` main() with a **two-tier** calibration (the providerSpof latent-SPOF precedent): a flag in the manifest's `acknowledgedFailOpenFlags` (known env-ON, held OFF today only by a live DB row; the deploy fix is a T3/Carson chain-adjacent change) is a non-blocking `::warning::`; a **NEW, unacknowledged** fail-open flag — or any `launch-flag-off` / `env-flag-on-no-db-guard` — is a hard **ERROR** that fails CI. The live tree sets `ENABLE_SEMANTIC_SEARCH=true` + `ENABLE_AI_FRAUD=true` in `deploy-worker.yml` while both are asserted effective=`false`; both are acknowledged (warnings) so the gate is green at rest, the hazard is auditable, and the regression guard is live. Recommended fail-safe follow-up (T3 / Carson): set `ENABLE_SEMANTIC_SEARCH=false` + `ENABLE_AI_FRAUD=false` in `deploy-worker.yml` so they fail SAFE regardless of the DB row, then drop them from `acknowledgedFailOpenFlags`. The **CSP-vs-runtime-deps** dimension (a CSP-breaking dependency — the 06-16 §1.6 regression) is owned by WEBEXT-04's `scripts/ci/check-csp-runtime-deps.ts` (PR #1262); this gate covers the CSP **connect-src** allowlist (bidirectional, above), so the two are complementary, not duplicated. 13 tests (`flagSpof.test.ts`) incl. real-tree smoke, plus the wiring + regression-guard tests in `check-config-drift.test.ts`.
6. ✅ **Provider-default SPOF — DONE (S1.5).** `config-drift/providerSpof.ts` parses the REAL `config.ts` default (`mempool`) + `deploy-worker.yml` override (`getblock`) and is wired into `check-config-drift.ts` main(): a dropped env line (silent fallback to a non-asserted provider) **or** a wrong override **fails CI**; a latent code-default divergence the deploy currently masks surfaces as a non-blocking `::warning::`. 11 tests (`providerSpof.test.ts`) incl. real-tree smoke. Fail-safe follow-up (recommended): align the `config.ts` default to the asserted provider so a dropped override fails safe — a chain-touching change, T3 / Carson-gated.

## Flag reconciliation — `flagInventory.ts` + `flag-inventory.json` (2026-08-11)

The third SPOF-class module here, and the one that made the other two honest. **`flagSpof` only ever saw the flags pinned in `expected-prod-config.json.flags` — 6 out of a ~51-flag code surface, so 45 flags were invisible to every config gate in the repo.** It also reasons about one hazard direction only: the env var winning when a `switchboard_flags` row is **absent**. The inverse was live in prod and nothing caught it — when the row is **present** the deploy env var is **inert**, so `deploy-worker.yml` can sit there stating the opposite of the effective value. It does: it sets `ENABLE_SEMANTIC_SEARCH=true` and `ENABLE_AI_FRAUD=true` while both switchboard rows are `false`. Both features are OFF. Every reader of the deploy workflow concludes they are ON.

`flag-inventory.json` is the **authoritative table**: for every flag, its sources, the code sites that actually resolve it, the live `switchboard_flags` row (`absent` is distinct from `false`), the value prod resolves through the real resolver, whether a customer can reach it, its soak posture, and why. `expected-prod-config.json` is what the drift/SPOF gates compare against; the inventory explains *why* each value is what it is. Keep them in step.

**Findings** (`error` blocks, `warn` is loud but non-blocking, mirroring the providerSpof precedent):

| Code | Sev | Catches |
|---|---|---|
| `unregistered-flag` | error | Code grew a flag the inventory does not name. The ratchet — a detector beats a census, which is why every hand-written flag list here has rotted. |
| `stale-inventory-entry` | error | Inventory names a flag with no code surface *and* no prod row. The inverse ratchet. |
| `env-db-contradiction` | error¹ | **The split-brain class.** DB-backed, row present, deploy env var says the opposite. Inert *and* misleading. |
| `inert-env-var` | warn | Same shape, env and row agree. Harmless today; the trap is that editing the deploy value does nothing. |
| `decorative-db-row` | warn | Worker resolves from ENV but a row exists that the frontend's `get_flag` **does** read — the two runtimes can disagree silently. Live today for `ENABLE_GRC_INTEGRATIONS` (row `true`, worker 503s). |
| `claimed-capability-off` | error¹ | **R-7 claims gate, automated.** Flag resolves OFF while a public/compliance surface states the capability is available. |
| `stale-claim-reference` | error | A `claimedBy` path that no longer exists — the annotation rotted and stopped protecting. |
| `soak-required-flag-off` | error | A `must-be-on` flag not resolving ON. A green soak with it off is silence, not evidence. |
| `soak-forbidden-flag-on` | error | A `must-be-off` flag ON (`USE_MOCKS`, demo injector, synthetic data, Nessie, maintenance mode). |

¹ Downgraded to `warn` when listed in `acknowledgedInertEnvFlags` / `acknowledgedClaimContradictions`. A **new** one is a hard error — that is the regression guard.

**`claimed-capability-off` is the highest-value check here.** It catches R-7 violations (CLAUDE.md §1.13) mechanically instead of at annual audit. Two are live and acknowledged: `ENABLE_SEMANTIC_SEARCH` is OFF while `src/pages/DevelopersPage.tsx:65` lists `POST /ai/search` at **$0.010 per call** — a priced commercial offer for an endpoint that 503s — and `ENABLE_AI_FRAUD` is OFF while `docs/compliance/soc2-type2-evidence-matrix.md` CC3.3 asserts fraud detection to a SOC 2 auditor as a **Continuous** control. **Fix these by correcting the copy/doc, never by flipping the flag to make a marketing claim true.**

**`launchRequiredFlags` is now an explicit set.** `checkFlagSpof` used to equate "asserted effective=true" with "launch-required", so honestly pinning any true flag held ON by its switchboard row fired a spurious `launch-flag-off` — the deploy correctly omits those env vars. The gate punished honest pinning, which is precisely why this manifest stayed at 6 entries and its prose rotted. Only flags in `launchRequiredFlags` can produce `launch-flag-off` now; omitting the set preserves the old behaviour.

**Two flags are declared but gate nothing** and should be deleted rather than left ambiguous: `ENABLE_COMPLIANCE_ENGINE` appears only in the `DB_FLAGS` array (no gate, no consumer — `/compliance/check` is gated by `x402PaymentGate` alone, so reading it as `false` invites the wrong conclusion that the surface is dark), and `ENABLE_ZK_PROOFS` is a live `true` switchboard row with **zero** code references anywhere.

**Known limitation, stated plainly:** `prodDbRow` / `prodResolved` are a stamped capture (prod ref `vzwyaatejekddvltxyye`, 2026-08-11, worker `2de4e4e34`), not a live read — CI has no prod credentials. What the gate mechanically enforces is that the **code** surface has not drifted from the reconciled inventory, plus soak posture and claims. Note `GET /health` does **not** expose the flag registry (verified live), so README item 2's "capture `/health` + the flag registry" needs a flag block added to `/health` first.

## `pendingLaunchFlags` — staged pins (WH-6, SCRUM-2899)

`expected-prod-config.json` may carry a `pendingLaunchFlags: string[]` key for launch-required flags that are specced/coded but **not yet flipped on in prod**. It is INERT: `check-config-drift.ts` reads it nowhere (Zod `.passthrough()` tolerates it; neither `diffConfigState` nor `flagSpof` consumes it), so it does not affect the gate. A flag here is NOT in `flags`/`launchRequiredFlags` on purpose — pinning it there while `deploy-worker.yml` still omits it would fire `flag-SPOF launch-flag-off` + a drift `absent from running` and turn CI red before the flag is actually live. To **activate** (Carson, post-soak + prod flip): move the flag into `flags: true` + `launchRequiredFlags`, add it to `prod-config-snapshot.json` `flags: true`, and add `<FLAG>=true` to `deploy-worker.yml --set-env-vars` so it fails SAFE. First entry: `ENABLE_OUTBOUND_WEBHOOKS` (webhook delivery kill switch).

Second entry: `ENABLE_PARTNER_PROVISIONING` (SCRUM-2990 — gates the reserved `/api/partner-provisioning` surface via `partnerProvisioningGate.ts`; get_flag RPC, fail-closed, no env fallback, so an unseeded row = 404-dark). Unlike WH-6 this is a launch-DARK flag, not launch-required: at partner-provisioning launch, release-ops seeds the `switchboard_flags` row first, then pins the effective value in `flags` (it does NOT go in `launchRequiredFlags` until the surface is actually launch-critical).

> **STALE, corrected 2026-08-11.** The paragraphs below described the state when each entry was added; 4 of the 5 had since gone live and the list could not be cited as evidence. `ENABLE_OUTBOUND_WEBHOOKS` has had a `switchboard_flags` row set `true` since 2026-08-01; the three Drive flags have been `=true` in `deploy-worker.yml` since 2026-08-03 and the deploy has since run. All four are now pinned in `flags`. **`ENABLE_PARTNER_PROVISIONING` is the only genuinely pending entry** — no row exists, `partnerProvisioningGate` uses `get_flag` with no env fallback, so `/api/partner-provisioning` is 404-dark. That surface is the HakiChain onboarding path. The text below is kept for the activation procedure, which is unchanged.

Third/fourth/fifth entries (2026-08-03, GH #1835/#1836/#1837): `ENABLE_DRIVE_OAUTH`, `ENABLE_DRIVE_WEBHOOK`, `ENABLE_DRIVE_CHANGES_RUNNER`. A variant of the WH-6 shape, inverted: the env vars are already `=true` in `deploy-worker.yml` as of the PR that added this entry (the connector's code fixes merged separately in #1944; only the flags were still dark), but they are held out of `flags`/`launchRequiredFlags` until the deploy has actually run and the Google Drive connector is confirmed working end-to-end in prod (OAuth connect, webhook receipt, changes processing) — see that PR's verification runbook. Pinning `true` here before that confirmation would assert prod state the authoring session could not verify (no `gcloud` access). `ENABLE_WORKSPACE_RENEWAL` is deliberately NOT part of this set — despite its name and its grouping alongside the other Drive flags in `flagRegistry.ts`, it does not gate the actual `drive-subscription-renewal` job (that job is unconditional, protected by a run lease rather than a flag) and was not touched by that PR; see the PR body for the full trace.

## Known SPIKE limitations (do not mistake for the mitigation)

- asserted + running are hand-authored committed JSON → the *CSP / fee* dimensions are green at rest and cannot yet see a *real* prod divergence. The **provider** dimension reads real source via `providerSpof.ts` (item #6), and the **flag env↔DB fail-open** dimension reads real source via `flagSpof.ts` (item #5) — both now catch a real source-side regression at PR time; the remaining gap is the *running*-side live capture (Sprint-1 `/health` cron);
- parity validates the committed runtime declarations, not a live worker-vs-edge capture;
- the CSP list is a worker+edge subset of the full `vercel.json` connect-src.

All three are resolved by the Sprint-1 source-parse + live `/health` capture above.
