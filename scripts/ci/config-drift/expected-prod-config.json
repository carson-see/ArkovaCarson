{
  "_note": "ASSERTED = intended EFFECTIVE prod config. For kill-switch flags the effective value is the DB (switchboard_flags) resolution, NOT the raw deploy env var: e.g. ENABLE_SEMANTIC_SEARCH and ENABLE_AI_FRAUD are env=true (deploy-worker.yml) but DB=false/gated (fail closed) => effective=false; both are pinned here so a silent re-enable is caught. cspConnectSrc is the cross-runtime-relevant SUBSET (worker+edge), not the full vercel.json connect-src. SPIKE: hand-declared; Sprint 1 captures env AND DB per flag so the env<->DB delta (the 2026-05-30 fail-open class) becomes the compared dimension. Provider 'getblock' = post-2026-04-25 sovereign-broadcast posture.",
  "_flagSpofNote": "S1 Lane-2: flagSpof.ts parses the REAL deploy-worker.yml env flags + the REAL flagRegistry.ts DB_FLAGS list and classifies env<->DB drift against `flags` below. flagRegistry resolves a DB-backed flag to its env var when the switchboard_flags row is ABSENT, so a flag asserted effective=false but env=true fails OPEN. `launchRequiredFlags` = effective ON; the gate hard-fails if the deploy sets it false/omits it (launch path break). `acknowledgedFailOpenFlags` = flags KNOWN to be env=true while asserted=false, held OFF today only by a live DB row (deploy edit is a T3/Carson chain-adjacent change). These surface as a loud non-blocking ::warning:: (the providerSpof latent-SPOF precedent); a NEW unacknowledged fail-open flag is a hard ERROR (the regression guard). instant-secure stays HIDDEN until QUEUE-08+ledger pass T3 (launch-gating, PRD §11) — it is a product surface, not a single env flag, so it is asserted via the queue/checkout flags rather than a fabricated ENABLE_INSTANT_SECURE key.",
  "_orgCreditEnforcementNote": "G4 (PR #1614): ENABLE_ORG_CREDIT_ENFORCEMENT is ENV-backed (flagRegistry ENV_FLAG_GETTERS via config.enableOrgCreditEnforcement — unlike ENABLE_PARTNER_PROVISIONING in pendingLaunchFlags, which is DB-backed). Pinned false here + in prod-config-snapshot.json: drift-clean today (env omitted in deploy-worker.yml), and any pre-G3 ENABLE_ORG_CREDIT_ENFORCEMENT=true added to deploy-worker.yml fails flag-SPOF as env-flag-on-no-db-guard ERROR. Do not flip before HakiChain funding (G3, founder-owned); the 0363 switchboard row is an audit mirror only, not the gate.",
  "flags": {
    "ENABLE_PROD_NETWORK_ANCHORING": true,
    "ENABLE_AI_EXTRACTION": true,
    "ENABLE_VERIFICATION_API": true,
    "ENABLE_SEMANTIC_SEARCH": false,
    "ENABLE_AI_FRAUD": false,
    "ENABLE_ORG_CREDIT_ENFORCEMENT": false
  },
  "launchRequiredFlags": ["ENABLE_AI_EXTRACTION", "ENABLE_PROD_NETWORK_ANCHORING", "ENABLE_VERIFICATION_API"],
  "acknowledgedFailOpenFlags": ["ENABLE_SEMANTIC_SEARCH", "ENABLE_AI_FRAUD"],
  "_pendingLaunchFlagsNote": "SCRUM-2899 (WH-6): ENABLE_OUTBOUND_WEBHOOKS is the webhook-delivery kill switch. It is DB-backed (flagRegistry.ts DB_FLAGS) and OFF in prod today (deploy-worker.yml omits it → registry env-fallback resolves false). This PR STAGES the pin here but does NOT enforce it: it is intentionally NOT in `flags`/`launchRequiredFlags` because that would fire flag-SPOF `launch-flag-off` (asserted ON, deploy omits it) and a drift `absent from running` — CI would go red before the flag is actually flipped. ACTIVATION (Carson, post-12h-T2-soak + prod flip): move \"ENABLE_OUTBOUND_WEBHOOKS\" into `flags: true` + `launchRequiredFlags`, add it to prod-config-snapshot.json `flags: true`, and add `ENABLE_OUTBOUND_WEBHOOKS=true` to deploy-worker.yml --set-env-vars so it fails SAFE. This inert key is ignored by check-config-drift.ts (Zod .passthrough(); not read by diffConfigState or flag-SPOF).",
  "_partnerProvisioningNote": "SCRUM-2990: ENABLE_PARTNER_PROVISIONING gates the reserved /api/partner-provisioning surface (partnerProvisioningGate.ts — get_flag RPC, fail-closed, NO env fallback; unseeded row = 404-dark, the safe default). DB-backed (flagRegistry DB_FLAGS). NOT pinned in `flags` yet per the WH-6 precedent above: no prod switchboard row exists (seeding is DBA/release-ops-owned), and pinning false would fire an `absent from running` drift. ACTIVATION (release-ops, at partner-provisioning launch): seed the switchboard_flags row, then pin the effective value here.",
  "_driveConnectorActivationNote": "GH #1835/#1836/#1837 (2026-08-03): ENABLE_DRIVE_OAUTH, ENABLE_DRIVE_WEBHOOK, ENABLE_DRIVE_CHANGES_RUNNER moved from unset to =true in deploy-worker.yml in the SAME PR that adds this note (the connector's code fixes had already merged in #1944 but the flags were never flipped, leaving it fixed-but-dark). Unlike the WH-6/SCRUM-2990 entries below, the env vars ARE now live in deploy-worker.yml as of this change — but they are deliberately NOT yet pinned in `flags`/`launchRequiredFlags`, because that requires the deploy to have actually run and the connector to have been confirmed working (see the PR's verification runbook), neither of which this PR itself can confirm (no gcloud in the authoring session). Pinning `true` here before that confirmation would assert prod state this PR cannot verify (CLAUDE.md §1.5/§1.13 R-7). ACTIVATION (post-deploy, once the verification runbook passes): move all three into `flags: true`, add them to `prod-config-snapshot.json` `flags: true`. Env-only flags (not in flagRegistry.ts DB_FLAGS), so they classify as `env-flag-on-no-db-guard` if ever pinned `false` here while still `true` in deploy-worker.yml — that is the correct fail-closed behavior, not a bug. Deliberately EXCLUDED from this list: ENABLE_WORKSPACE_RENEWAL. It does not gate the drive-subscription-renewal job (which is unconditional, run-lease-guarded — see services/worker/src/jobs/drive-subscription-renewal-deps.ts) and was not added to deploy-worker.yml by this PR; see the PR body for the full trace of what it actually gates instead (an unrelated, permanently-stubbed SCRUM-1147 job with no scheduler trigger).",
  "pendingLaunchFlags": ["ENABLE_OUTBOUND_WEBHOOKS", "ENABLE_PARTNER_PROVISIONING", "ENABLE_DRIVE_OAUTH", "ENABLE_DRIVE_WEBHOOK", "ENABLE_DRIVE_CHANGES_RUNNER"],
  "bitcoinUtxoProvider": "getblock",
  "bitcoinFeeStrategy": "mempool",
  "cspConnectSrc": [
    "https://arkova-worker-270018525501.us-central1.run.app",
    "https://edge.arkova.ai"
  ],
  "runtimes": {
    "worker": {
      "runtime": "worker",
      "origin": "https://arkova-worker-270018525501.us-central1.run.app",
      "flags": { "ENABLE_AI_FALLBACK": false },
      "bitcoinUtxoProvider": "getblock"
    },
    "edge": {
      "runtime": "edge",
      "origin": "https://edge.arkova.ai",
      "flags": { "ENABLE_AI_FALLBACK": false }
    }
  }
}
