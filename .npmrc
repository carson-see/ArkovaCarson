legacy-peer-deps=true
# DEP-15 / SCRUM-1005: pin every install to an exact version so no
# caret or tilde ranges leak into package.json on a fresh `npm install
# --save <pkg>`. Enforced post-hoc by scripts/ci/check-dep-pinning.ts.
save-exact=true
