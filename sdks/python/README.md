# Deprecated Python SDK Copy

This directory is intentionally no longer a Python package.

The canonical Arkova Python SDK lives at
[`packages/arkova-py`](../../packages/arkova-py). The publish workflow,
API docs, and `pip install arkova` package all point there.

This README is kept only so older links fail loudly instead of silently
inviting new work into a stale duplicate source tree.
