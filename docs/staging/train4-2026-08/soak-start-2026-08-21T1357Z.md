# TRAIN-4 — T2 soak stand-up, clock start 2026-08-21T13:57:35Z

**Rig:** `arkova-worker-wave3-2026-08-staging`, revision `00005-rib`, tag `train-4`
**Supabase:** `jiotjhqmedkajdsojsbn` (isolated)
**Union head:** `0333464d7b22e140c07b049718b1214ea98633e1`
**Image:** `arkova-worker-wave3@sha256:bbf04ffd06b994b8ebb163db5891e156dc3e3de4ae52d4317a8ecf37a3b2d279`
**Clock:** 2026-08-21T13:57:35.121840Z → **2026-08-22T01:57:35Z** (T2, 12 h)

## Members — heads FROZEN, do not restack

Per FD-RC-1 these PRs will carry **per-PR evidence blocks**, not an RC manifest, so each
head must stay exactly as recorded here.

| PR | Frozen head | Surface |
|---|---|---|
| #2211 | `7143daebf55f` | ORG_ADMIN-gate the self-serve verification flow |
| #2220 | `9f67e98b6c09001f080b12dcc23301c0b206dfb2` | FD-P7 — API-key revocation/deletion reachable |
| #2230 | `274ad17eb9af17f910b9b39d39d50434672d1928` | Drive connect: `org_scope_required` vs `not_admin` |
| #2232 | `7b7a7d42abe24adb3b72e5566e5a1cc516186459` | MCP audit log + unmounted proof-keys route |
| #2233 | `1d1ab20a4a0e` | Ingestion must not report total failure as HTTP 200 |
| #2236 | `945b4a64ddd3a8edadc7c963a6a94614a71b6ec3` | Claims: Nessie fails closed |

All six touch `services/worker`, so a worker rig can genuinely exercise them — unlike
wave3's `sdk-py`/packaging members. **Union merged with zero conflicts**; root
`npm run typecheck` exit 0.

## Why this rig

Its `switchboard_flags` already carries **`ENABLE_VERIFICATION_API = true`** (re-verified,
not assumed). That single row is the difference between wave3's real `/api/v1` evidence and
wave2's worthless sub-10 ms fail-closed 503s.

## Health at clock start

5/5 × HTTP 200 (1.281 s, 0.562 s, 0.276 s, 0.272 s, 0.278 s).

```json
{"status":"healthy","git_sha":"0333464d7b22e140c07b049718b1214ea98633e1",
 "checks":{"database":"ok","anchoring":"ok","kms":"warning"}}
```

`git_sha` equals the union head exactly (deployed with an explicit
`--update-env-vars BUILD_SHA=…`, without which `/health` reports the *previous* image's
SHA — the trap hit on #2290).

### `kms: warning` is NOT a train regression

The other two rigs report `kms: ok`, so this warranted checking rather than waving off. It
is a **pre-existing rig config difference**: the pre-train revision `00004-cjk` has
byte-identical env — no `KMS_PROVIDER`, no `BITCOIN_TREASURY_WIF` — while chain-pair
carries `KMS_PROVIDER=gcp`, `BITCOIN_TREASURY_WIF` and Bitcoin RPC secrets.

**Coverage limit recorded up front:** this rig cannot exercise signing, treasury or
broadcast paths. Any member behaviour touching them is out of scope for this window and
must not be claimed.

## Two driver defects found and fixed before the clock was trusted

**1. Token audience — every request was 401.** The driver minted with
`--audiences="${BASE_URL}"`. Against a *tag* URL, Cloud Run rejected the resulting token:
38 × 401 on `/health`, 13 × 401 on `/api/v1/verify/…`, 8 × 401 on `/.well-known/…`,
401 on both cron routes. Manual curls with a plain identity token returned 200 against the
same URL. Fixed by dropping `--audiences`.

**2. The serious one — 401 was counted as SUCCESS.** The classifier treated any 2xx–4xx as
`ok`, so the fully-broken run above reported **`ok: 61, fail: 0`**. A soak driven by that
driver would have produced 12 h of "passing" evidence in which every single request was
rejected — precisely the hollow soak FD-WAVE3-1 describes, one layer deeper. 401/403 now
count as **fail**.

Verified after both fixes, same URL, same 90 s window:

| | before | after |
|---|---|---|
| `ok` | 61 | 53 |
| `status_200` | **0** | **52** |
| `fail` | 0 | 0 |

52 real authenticated 200s where there had been none. This also confirms the union
introduces no regression on `/health`, `/api/v1/verify` or the cron routes.

## What must still be driven — the wave3 lesson

wave3 soaked nine members and probed two. The current driver covers `/health`,
`/api/v1/verify/…`, `/.well-known/arkova-keys.json`, `/jobs/queue-digest` and
`/jobs/platform-health-digest` — which does **not** reach most of this train's surfaces.

Before this window closes, the driver must additionally probe:

- **#2220** — API-key revocation/deletion endpoints
- **#2230** — Drive connect deny-reason paths (`org_scope_required` vs `not_admin`)
- **#2232** — MCP audit-log write path and the previously-unmounted proof-keys route
- **#2211** — the ORG_ADMIN-gated self-serve verification flow
- **#2233** — ingestion failure-status path (must not return HTTP 200 on total failure)
- **#2236** — Nessie fail-closed claims

**Any surface not actually probed will be written up as NOT covered in that member's
evidence block.** No blanket window claims.
