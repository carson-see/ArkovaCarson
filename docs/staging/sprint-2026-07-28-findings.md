# Sprint findings → backlog (file in Jira at housekeeping wave)
_Everything discovered during the 2026-07-28 final sprint that is NOT already a work item. Atlassian MCP write path was flaky 07-27 — batch these at close._

## New defects found mid-sprint
1. **`public/AGENTS.md` MCP undercount** — same 2-of-16 tool undercount as `server-card.json` (fixed in #1726). Markdown, not mechanically diffable; fast-follow. Found by #1726 builder. Priority: Medium.
2. **`merge.union.driver=true` in local `.git/config`** — ROOT CAUSE of the months-long "agents.md union merge silently drops sections" incidents (CLAUDE.md §6 mistake table, HANDOFF 07-12 + 07-27, PRs #1615/#1652). Shadowed git's built-in union driver with the shell command `true` → clean-looking merge, incoming side discarded. FIXED 2026-07-28 (`git config --local --unset merge.union.driver`), verified by scratch-repo test. Main was never corrupted (server-side Mergify merges used the real driver). **Backlog items:** (a) correct CLAUDE.md §6's mistake-table entry, which currently misattributes this to git/ort behavior; (b) audit PR branches that locally merged main in before the fix; (c) consider a repo-level guard (a CI check or documented setup step) so a future clone can't reintroduce it. Priority: High (docs correctness + recurrence prevention).
3. **Two competing `invite_member()` RPC overloads** in baseline SQL (~lines 4349 and 4405) — differ only by signature; CLAUDE.md §6 explicitly bans overloads differing only by DEFAULT. Found by L2-A4 builder (using the SEC-RECON-8-guarded one). Needs a compensating migration to drop the dead overload. Priority: Medium.
4. **`zk-proof.test.ts` requires locally-compiled circuit artifacts** — fails in fresh worktrees/CI-less envs (pre-existing env gap, not a regression; seen by multiple agents). Should either ship fixtures, skip cleanly with a clear message, or be documented. Priority: Low.
5. **`scripts/vendor-ner-runtime.test.ts` + `scripts/staging/load-harness-env.test.ts`** fail in isolated worktrees — missing `node_modules/@huggingface/transformers` and `node_modules/tsx` despite being in package.json (workspace hoisting issue). Priority: Low.

6. **GitHub Actions heredoc-injection pattern (repo-wide)** — `$GITHUB_OUTPUT` multiline writes using a FIXED heredoc delimiter fed from author-controlled content allow forged `key=value` injection (later key wins). Found by review on #1724's new step; a PRE-EXISTING instance also lives in `.github/workflows/ci.yml` "Aggregate commit messages" (~line 330, fed from commit messages). Both being fixed in #1724 (per-run random delimiter). **Backlog:** sweep ALL workflows for `>> "$GITHUB_OUTPUT"` heredocs with static delimiters and add a CI lint forbidding the pattern. Priority: High (security).
7. **`merge_commit_sha` can be stale-but-present** — GitHub nulls + async-recomputes it when the base moves; code that only guards against NULL still consumes stale values. Affects staging-evidence gate resolution. Remedy: check `mergeable_state` (`dirty`/`unknown` → fall back to head SHA). Priority: Medium.
8. **Duplicate live-PR fetches in the evidence gate** — `fetchLiveLabels()` (`scripts/ci/lib/ciContext.ts:190-212`) issues its own `gh api .../labels` call at TS runtime, separate from the bash `live_pr` step's `gh api .../pulls/<N>` — two calls at two instants, so labels can disagree with head/base/body; bash heredoc parsing also duplicates logic `ciContext.ts` does more safely. Consolidate. Priority: Medium (correctness seam + redundant I/O).
9. **`paymentTierRouter.ts` is not wired into `index.ts`** — tested in isolation only, never mounted. Pre-existing; found during the billing-idempotency audit (#1727). Mirrors the `perOrgRateLimit` unmounted-dead-code finding from S3.3. Priority: Medium — decide mount-or-delete.
10. **`*.test.sh` files never run in CI** — 8 shell test files exist (`scripts/staging/deploy.test.sh`, `scripts/agent/check-claude-bootstrap.test.sh`, the new `mint-fresh-event.test.sh`, …) with no discovery mechanism in `ci.yml` or `package.json`. Their assertions are dead weight — drift in those scripts is uncaught. Priority: Medium — add a shell-test runner job.

11. **83 pre-existing ruff violations on main** under the CI-resolved ruff (0.16.0) — mostly legacy typing syntax in `packages/arkova-py/.../proofs.py`. The PyPI publish workflow gates on ruff and a ruff error broke that publish once before (#1506). Since SDK publish is a founder demand, this is a live launch hazard. Found during #1729. Priority: High (blocks SDK publish once accounts exist).
12. **Dangling `memory/feedback_*.md` citations** — CLAUDE.md and new docs cite ~dozens of `memory/feedback_*.md` paths; the repo's committed `memory/` dir contains only `README.md` + `feedback_nvi_lawyer_scope.md`. Systemic gap between session-memory and the repo. Either commit the referenced rules or stop citing them as repo paths. Priority: Low-Medium (doc integrity; auditors will follow these links).
13. **Orphan-lint inverse gap** — a PR deleting the LAST importer of an existing hook/component makes it newly unreachable but only ever WARNs (never fail-closed), because classification keys off the orphan's own file appearing in the diff. Same incident class as folders-without-UI. Being addressed in #1723 if cheap; else file. Priority: Medium.
14. **Non-concurrent `CREATE INDEX` inside a txn with `ALTER TABLE`** (`0359`) — inherits ACCESS EXCLUSIVE for the index build, blocking all reads/writes on `anchor_proofs` until commit. Low-risk at apply time (~6,110 rows) but the materializer backfill grows that table to ~2.96M, so the NEXT migration using this template is dangerous. Siblings 0365/0366 correctly split `CREATE INDEX CONCURRENTLY` out of the wrapper. No CI check catches this class. Priority: Medium — add a migration lint.

## Verify-and-close (no build needed — confirm then transition)
- **SCRUM-2486** — CTID invariance: enforcing trigger `0357` already merged to main and below prod ledger head 0366.
- **SCRUM-2603** — public verify throttle: already fixed on main by commits `7ed0f687`/`7372ee19` (adminRouter no longer swallows `/api/v1`). L2-A7 covers the legacy `/api/verify-anchor` residual.
- **SCRUM-3028** — E2E live mempool.space calls: fixed by #1713 (merged).
- **SCRUM-2492** — §1.6A connector-byte hardening: already on main (`byte-safety.ts`, pino/Sentry scrubbers, `arkova/no-connector-bytes-to-sink` eslint rule). Currently sits in Needs Human, self-labeled connector launch-blocker — stale.
- **SCRUM-3014** / **SCRUM-3005** / **SCRUM-2975** — already Done in Jira, confirm no residual.

## Deferred (file with explicit rationale, do NOT silently drop)
- Member-level credit delegation (org admin → member) — new money-path primitive, no prior design doc. CTO R8: post-soak / Sprint C.
- Full 3-ledger consolidation backfill (`credits` / `org_credits` / `unified_credits`) — R4 names `org_credits` canonical and stops NEW divergence; historical merge is its own T3 effort.
- undici 7→8 major bump — closed as #1717; real API break (`webidl.util.markAsUncloneable`) in `safe-fetch.ts`'s use of an undici internal. Post-launch: migrate off the internal, then take v8.
- Bulk Instant-Secure (multi-doc credit spend) — deferred past S6 in the original pre-design; single-doc only.
- W5–W8 bulk surfaces: MCP anchor tools, edge-MCP array support + pipeline disambiguation, DocuSign mixed-format envelope verify, Drive batch-trigger UI. Sized in the bulk audit; schedule into Sprint B as capacity allows.
- MCP registry submission (registry.modelcontextprotocol.io) — needs an account; founder-adjacent.
- CTDL xTRA RFP — do-not-bid ratified (R11).
- SonarCloud "review as safe" clicks — founder-side; Sonar MCP returns Not authorized in agent sessions.

## Founder-reserved (surface in the report, do not build)
npm/PyPI accounts + tokens (SDK publish) · Resend domain/DNS verification · SIGN-01 proof-signing key decision · flag flips (`ENABLE_OUTBOUND_WEBHOOKS`, `ENABLE_ORG_CREDIT_ENFORCEMENT`, `ENABLE_CONNECTOR_ARTIFACT_DRAIN`, `ENABLE_QUEUE_DIGEST`) · drain decision D1 (255k backlog) · materializer prod-EXECUTE scheduling (after SCRUM-2983 backup drill + SCRUM-2984 autovacuum readiness) · HakiChain invoice/LOI + Kenyan counsel · CE demo CTID from Jeanne · mystery rig `arkova-worker-s33-rig-b1-staging` origin check · daily-digest send time (6pm EST fixed vs America/New_York DST-aware).

## Dependabot posture (verified 2026-07-28, pre-pentest)
GitHub's push banner claims "45 vulnerabilities (1 critical, 27 high)". The **API disagrees**: only **7 OPEN** alerts — 1 critical, 4 high, 2 medium. The banner appears to count non-open/historical alerts; do NOT quote the 45 figure in any security or partner document.
**Every critical/high is `development` scope** (not production runtime): `tar` (critical, DoS via unlimited input), `tar` (high, negative entry size → infinite loop), `brace-expansion` (high, unbounded expansion DoS), `undici` (high, WebSocket DoS). Dev-scope = build/CI supply-chain risk, not attack surface on the running service — an external pen test of the product will not surface these.
**Actions:** (a) bump the dev-scope four before the pen test so the report is clean; (b) the `undici` high is the same package #1717 tried to bump and was closed as deferred — dev-scope means that call stands, but it is now a concrete reason to do the `safe-fetch` migration off undici internals post-launch; (c) capture this verified 1-critical/4-high/2-medium breakdown in the SOC2 evidence pack rather than the misleading banner number.

## Later additions (2026-07-28, cont.)
15. **`/api/v1/anchor/bulk` was BROKEN, not merely unwired** — its insert never set `filename`, which is `NOT NULL` at the DB layer, so every real (non-mocked) call would 500. The fully-mocked test suite stayed green. Fixed additively in #1738. **Lesson for the backlog:** mocked-DB tests hid a NOT NULL violation on a production endpoint — consider a contract/integration test tier that exercises real schema constraints for `/api/v1` write paths. Priority: High (test-strategy gap, not just this bug).
16. **`database.types.ts` hand-edited in #1739** rather than regenerated (0375 isn't applied to any ledger yet). Must be regenerated via `gen:types` once 0375 applies. Priority: Medium — flagged in agents.md, don't let it drift.
17. **Org-admin cannot remove a member's queued item** — no org-admin UPDATE/DELETE policy on `anchors`; `anchors_select_org` is also not role-gated at the RLS layer (only app-side query construction restricts it). Found by #1737, which disabled the control rather than widening RLS. Belongs with SCRUM-3010. Priority: Medium-High (founder spec says admins can remove queue items).
18. **Concurrent-agent preview-port contamination** — multiple agent worktrees on one host collide on localhost:5173; one session's preview rendered another's in-progress feature. Caused two PRs (#1737, #1739) to honestly skip UAT screenshots. Need per-worktree port assignment for parallel UI work. Priority: Medium (blocks UAT evidence at scale).
19. **DocuSign connector card lacks last-sync/document-count** — identical gap to the Drive one closed in #1654. Priority: Low-Medium.
20. **Absolutely-positioned file `<input>` swallows clicks** in the upload drop-zone (CSS painting order) — affects the pre-existing "Remove file" button even while disabled. Found live via Playwright during #1736 (which fixed its own new container with `relative z-10`). Priority: Medium (real user-facing click failure).
21. **LGPL-3.0 transitive dep `libheif-js`** (via `heic-decode`, PR #1740) in the FRONTEND bundle. Lazy/dynamically imported, which is the LGPL-friendlier posture, and direct deps are permissive (utif2 MIT, heic-decode ISC, upng-js MIT). Needs a one-time counsel/founder sanity check for a commercial product. Fallback if unacceptable: drop HEIC back to benign soft-fail (costs 1 of the 22 formats). **FOUNDER/LEGAL decision, not CTO.** Priority: High (blocks nothing today; must resolve before launch marketing claims 22/22).
22. **Orphaned worker devDependencies** — `services/worker/package.json` declares `pdfjs-dist` and `tesseract.js` with ZERO importers in the worker (they belong to the client-side §1.6 path). Dead weight + confusing §1.6 signal. Priority: Low.
23. **#1735 and #1740 both edit `src/lib/ocrWorker.ts`'s dispatch chain** — guaranteed merge conflict; RTE must sequence them (F2/F3 then F4, or vice-versa) and verify the dispatch switch retains BOTH sets of branches after resolution. Priority: sequencing note for the wave.
24. **`createPendingRecipient` (recipients.ts) may violate a FK** — inserts a `profiles` row with a fresh random UUID BEFORE any `auth.users` row exists, but `profiles.id` has `FOREIGN KEY … REFERENCES auth.users(id)`. Either it silently fails in prod, or the FK is not what the schema implies. Found during #1742; deliberately not touched (out of scope). Investigate before launch — recipient provisioning is on the issuance path. Priority: High (data-integrity, unclear current behavior).
25. **`CSVUploadWizard.tsx` is unreachable dead code** — only `BulkUploadWizard.tsx` is actually rendered. Found during #1741. Delete or wire. Priority: Low.
26. **MIGRATION COLLISION (resolved in-session)** — #1739 and #1741 BOTH claimed `0375`; #1739 won on first-push (11:30:39 vs 11:33:20) and #1741 renumbered. Root cause: the reservation protocol relies on agents re-reading `agents.md` on origin/main, but parallel agents can claim within the same window. **The uniqueness lint only checks main, so it cannot catch open-PR-vs-open-PR collisions** (a known documented gap). Backlog: extend the lint to scan open PRs' migration files via `gh api`, so collisions fail at PR time rather than being caught by a human. Priority: High (this WILL recur with parallel agents).
