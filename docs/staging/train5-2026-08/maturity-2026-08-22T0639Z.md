# TRAIN-5 — T2 soak maturity record, clock closed 2026-08-22T06:39:17Z

**Rig:** `arkova-worker-fullsoak-2026-08-staging`, revision `00024-kaj`, tag `train-5`
**Tag URL:** `https://train-5---arkova-worker-fullsoak-2026-08-staging-kvojbeutfa-uc.a.run.app`
**Image digest:** `sha256:67431ad081747bb0277a074f55ea987854bf471b27ae9b89f35d79bc05cfebd4`
**Worker `git_sha` (from `/health`):** `dbe57d63856eb28b839b2dbfbaebcb9cab3b5938` (= union head)
**Base:** `224cef8a99eaeacc8cf535890f0dbfca1730d382`
**Supabase:** `gnkuaywlpmsaezwvlvhk` (isolated)
**Network:** `signet`
**Declared tier:** T2 — worker behaviour / public API / deploy config
**Revision clock start (FD-CLOCK-1):** 2026-08-21T18:39:17.404565Z
**Clock close:** 2026-08-22T06:39:17Z — **12.00 h, the full T2 minimum**
**Stand-up:** `docs/staging/train5-2026-08/soak-start-2026-08-21T1839Z.md`

## Verdict

**The clock holds, the load coverage is the cleanest of the 2026-08 windows, and the error
budget is effectively empty — but the window covers four of ten members directly and six only
partially or not at all, and two §1.12 requirements were not met.**

25,242 requests across 12.00 h, **zero gaps over 20 minutes** (largest gap in the whole window:
**21 seconds**), and **two 5xx in total**, both from the same deliberate probe with a fully
identified cause. Health at close is 5/5 × HTTP 200 with `git_sha` still equal to the union head.

What this window does **not** carry: a deploy rollback rehearsal, a `staging_deploy_log` row, and
direct coverage for #2245, #2258, #2270, and the specific changed branches of #2251, #2266 and
#2267. Those are enumerated below and repeated in each member's own evidence block.

## Clock integrity — PASSES, with one measured qualification

| Condition | Observed |
|---|---|
| Serving revision | `arkova-worker-fullsoak-2026-08-staging-00024-kaj` |
| Revision created | 2026-08-21T18:39:17.404565Z |
| Revision at close | **unchanged** — `creationTimestamp` re-read post-close, identical |
| Traffic at close | **100 %**, tag `train-5` (`gcloud run services describe`) |
| Revisions that served in-window | `00024-kaj` **25,217** + `00022-suy` **25** — see below |
| Container terminations on the serving revision | **none** |
| OOM / memory-limit events | **none** anywhere on the service |
| 5xx across the 12 h window | **2** (see below) |
| Health at close (post-close reading, 2026-08-22T15:21Z) | **5/5 × HTTP 200**, 0.275–0.484 s |

`/health` at close, verbatim:

```json
{"status":"healthy","version":"0.1.0","git_sha":"dbe57d63856eb28b839b2dbfbaebcb9cab3b5938",
 "uptime":74511,"network":"signet","checks":{"database":"ok","anchoring":"ok","kms":"ok"}}
```

That reading is **post-close** (08-22T15:21Z, ~8.7 h after the clock ended) and is labelled as
such. `uptime` 74,511 s is the current *instance*, not the clock (FD-CLOCK-1).

### The qualification: `00024-kaj` was not the only revision to serve in-window

25 of the window's 25,242 requests were served by the **previous** revision `00022-suy`
(tag `cp0819rc`), and the honest breakdown matters:

* **5 × `GET /health` → 200, 18:39:22.5Z … 18:39:44.9Z**, on the *bare* service URL. The
  revision was created at 18:39:17.404565Z but Cloud Run's traffic pointer did not move for a
  further ~40 s: the first request served by `00024-kaj` is at **18:39:57.3Z**. For the first
  40 seconds of the clock the bare service URL was still answering from the outgoing revision.
* **20 × 403** on the `cp0819rc---…` **tag URL** (`/` ×16, `/favicon.ico` ×4), scattered from
  18:42Z to 05:25Z. These are Cloud Run IAM rejections of unauthenticated external GETs — the
  worker never saw them. They are attributed to `00022-suy` because that is the tag's target.

Neither category is soak traffic and neither touched the tested code. But *"only the soak
revision served in-window"* is **not** literally true for this window, and the 40-second
traffic-flip lag also means the stand-up's "health at clock start" reading cannot be assumed to
have come from `00024-kaj` — the request log shows the first five bare-URL health 200s of the
window landing on `00022-suy`. The five `/api/health` 200s on the **train-5 tag URL** at
18:40:08–18:40:10Z are the earliest in-window health checks provably served by the tested
revision, and they were green.

### Revision `00025-rek` — created in-window, failed to start, took zero traffic

| | |
|---|---|
| Created | 2026-08-21T18:44:16.604773Z |
| Ready | **False** — `HealthCheckContainerError` |
| Message | container failed to start and listen on `PORT=3001` within the timeout |
| Traffic | **absent from the service's traffic list entirely** — 0 %, no tag |
| Boot attempts | 61 ERROR entries + repeated `Container called exit(1)`, 18:44:56Z → 18:47:27Z |
| Failure | `Error: Invalid worker configuration` at `loadConfig (dist/config.js:901)` |

Cause: `ENABLE_DRIVE_OAUTH=true` was set to try to reach #2245's surface, and this rig carries no
`GOOGLE_OAUTH_CLIENT_ID` / `GOOGLE_OAUTH_CLIENT_SECRET`, so `config.ts`'s Zod validation rejects
the combination at boot. Confirmed from the traffic split (`00025-rek` is not in it) and from the
per-revision error attribution: **all 61 startup errors carry `revision_name = …-00025-rek`; the
serving revision `00024-kaj` logged exactly 2 ERROR entries in the whole window, and both are the
two 503s below.** The clock was unaffected.

## Load coverage — 100 % of the clock, zero gaps over 20 minutes

Computed from Cloud Run request logs (`run.googleapis.com/requests`), not from local driver logs,
pulled in **12 × 1-hour slices**. Largest slice: **2,265 entries** — no slice came near the
20,000-entry read cap, so no total here is silently truncated.

| | |
|---|---|
| Requests in window | **25,242** |
| First request | 2026-08-21T18:39:22Z — **5.5 s** after clock start |
| Last request | 2026-08-22T06:39:14Z — **2.6 s** before clock close |
| Gaps > 20 min | **0** |
| **Largest gap in 12 h** | **0.34 min (21 s)**, at 08-21T18:46:01Z |
| Loaded coverage | **100.0 %** of the 12.00 h clock |
| 20-min buckets containing ≥1 request | **36 / 36** |
| Steady-state rate | 2,028–2,265 req/h (~34–38 req/min) |

Hourly counts, no dead hour: 555 (partial 18Z) · 2,096 · 2,028 · 2,055 · 2,080 · 2,069 · 2,049 ·
2,094 · 2,140 · 2,191 · 2,164 · 2,234 · 1,487 (partial 06Z).

The offered rate (36/min) sits under the **real** 60 req/min anonymous ceiling documented in
[[FD-LOAD-1]] — not merely under §1.10's stated 100/min. There are **zero 429s in this window**,
which is the empirical confirmation that the driver was sized correctly.

### Driver-side evidence

`docs/staging/train5-2026-08/evidence/load-*.json`, 39 files spanning 18:49:18Z → 10:42:20Z (the
driver was left running ~4 h past close). **In-window: 29 cycles, ok = 18,808, fail = 1.**

The single failure is one `2246-verify-absent=000 (want 404)` in cycle `20260821T214518Z` — a
connection-level failure with no HTTP status, one out of 18,809. It is recorded as a deviation in
the evidence file rather than smoothed away.

## The two 5xx, with cause

Both are `503` on `POST /api/v1/integrations/google_drive/oauth/start`, at **18:43:26.354Z** and
**18:43:26.759Z** (405 ms apart), latencies 46 ms and 4.8 ms.

**Cause:** the deliberate attempt to reach #2245's surface. The route answers
`503 integration_disabled … "flag":"ENABLE_DRIVE_OAUTH"` because the flag is off on this rig.
This is the flag gate working correctly, not a fault — but it is a real 5xx and is counted as one.

**Nothing else in the window returned 5xx.** Full status distribution:

| Status | Count | What it is |
|---|---|---|
| 200 | 19,365 | all probe and cron surfaces |
| 404 | 5,854 | **5,851** are the *declared* absent-record probe (`/api/v1/verify/{absent}`, #2246's lookup path — 404 is the correct answer); 1 on `/api/v1/nessie/query`; 2 other |
| 403 | 21 | 20 = Cloud Run IAM on the `cp0819rc` tag URL (above); 1 on `00024-kaj` |
| 503 | 2 | the #2245 probe, above |
| **429** | **0** | — |

## Per-member coverage at close — measured, not assumed

Re-verified against the 39 driver evidence files (every cycle's `memberProbes` string parsed) and
against the Cloud Run request log. Probe tallies below are **across all 39 cycles**.

| PR | Probe result | Verdict |
|---|---|---|
| **#2272** | `POST /jobs/queue-digest` → **200 × 39/39**; 149 requests in-window all 200. Live body at stand-up `{"admins":3,"sent":0,"skippedEmpty":3}` | **DIRECT.** `ENABLE_QUEUE_DIGEST=true` was set on the rig, so the activation is live rather than a no-op; default-on enrolment resolved 3 admins and the skip-empty branch fired. |
| **#2276** | `POST /jobs/platform-health-digest` → **200 × 39/39**; 149 in-window, all 200. Live body `{"admins":2,"failed":2}` | **DIRECT.** The new route is mounted and the job executes. The 2 failures are the rig having no mail transport, not the job. |
| **#2254** | `POST /jobs/pipeline-throughput-monitor` → **200 × 39/39** with `.linkerStallMinBacklog` and `.belowBacklogFloor` asserted present; `POST /jobs/db-health` → **200 × 39/39** (293 in-window) | **DIRECT.** Both new fields are the PR's count floor and both are present in the live response. `db-health` additionally exercises the cache-vs-live-probe path. |
| **#2246** | `/api/v1/verify/{present}` → 200 ×1,517; `/verify/{absent}` → 404 ×1,517; `/verify/{id}/proof` → 200 ×39; `/verify/{id}/provenance` → 200 ×39 | **DIRECT** on the proof-packet, provenance and verify surfaces, including the absent-record branch. |
| **#2267** | `GET /api/v1/webhooks` → 200 ×39; `GET /api/v1/nessie/query` → 200 ×39 | **PARTIAL.** Two swept modules exercised on their **read** paths only. The surrogate-safe truncation manifests on a **write** payload, which was never driven. |
| **#2266** | `POST /jobs/anchor-public-records` → 200 ×39 (221 in-window) | **PARTIAL.** The job runs green every cycle, but with `processed: 0` the poison-record and quarantine branches are never entered. |
| **#2251** | `GET /api/v1/usage` → 200 ×39; quota headers observed live (`x-quota-used: 1`, `x-quota-limit: unlimited`, `x-quota-reset`) | **PARTIAL — ALLOW half only.** FD-RL-2's claim is about a **denied** request. The driver org is ENTERPRISE/unlimited, so no denial was reachable. **The denial path is NOT soak-covered.** |
| **#2270** | — | **INDIRECT.** Every `/jobs/*` probe runs the changed Sentry cron-check-in gate on a non-prod service, so the new branch executes ~1,300 times; the suppression itself is not HTTP-observable and is **NOT asserted**. |
| **#2258** | — | **NOT PROBED.** The Sentry `event.extra` scrubber has no HTTP surface. Unit tests only. |
| **#2245** | `POST /api/v1/integrations/google_drive/oauth/start` → **503 `integration_disabled`** | **NOT COVERED, and not coverable on this rig.** See below. |

### #2245 is NOT soak-covered

The Drive OAuth start route is gated behind `ENABLE_DRIVE_OAUTH`, which is off on this rig; the
two attempts returned `503 integration_disabled`. Turning the flag on produced revision
`00025-rek`, which crash-looped on boot (`Invalid worker configuration`) because the rig has no
`GOOGLE_OAUTH_CLIENT_ID` / `GOOGLE_OAUTH_CLIENT_SECRET`. The failed revision took zero traffic
and the clock was unaffected.

**#2245's changed behaviour — the scope set that `buildAuthorizationUrl` mints — was never
executed on this rig during this window.** It is recorded as NOT covered in its own evidence
block. `arkova-worker-wave2-2026-08-staging` carries both Google OAuth secrets and is the rig
that could cover it.

## Preflight — quoted verbatim from the stand-up

`scripts/ci/staging-honesty-preflight.ts` was run to completion against `gnkuaywlpmsaezwvlvhk`
with a real Supabase Management API token. **Result: `environment_type = clean_mirror`, exit 0,
all checks passed.**

```json
{
  "environment_type": "clean_mirror",
  "staging_project_ref": "gnkuaywlpmsaezwvlvhk",
  "timestamp": "2026-08-21T19:04:14.545Z",
  "checks": [
    { "name": "staging_only_rows",  "passed": true, "details": "No PR-only or staging-only migration rows found." },
    { "name": "duplicate_names",    "passed": true, "details": "No duplicate migration names." },
    { "name": "duplicate_versions", "passed": true, "details": "No duplicate migration versions." },
    { "name": "known_artifacts",    "passed": true, "details": "No known artifact rows." },
    { "name": "submitted_anchors",  "passed": true, "details": "108 SUBMITTED anchor(s) found." },
    { "name": "prod_divergence",    "passed": true, "details": "Rig ledger reconciles with repo migration files + canonical baseline." }
  ],
  "artifact_rows": [],
  "missing_from_staging": []
}
```

(`extra_vs_prod` is omitted here for length and is informational only — the script's hardcoded
`DEFAULT_PROD_VERSIONS` stops at `0297`, so every later migration lands in that bucket. The
PASS/FAIL verdict comes from `analyzeProdDivergence`, which reconciles. The full array is in the
stand-up doc.)

**Two things about this preflight are not clean, and are stated rather than rounded:**

1. **It ran at 19:04:14Z — 24 min 57 s AFTER the clock started at 18:39:17Z.** §1.11A says to run
   it *before* starting a T2/T3 soak, and `check-staging-evidence.ts` enforces
   `Preflight timestamp <= Soak start`. It does not, so every member's evidence block will trip
   that specific rule. The reading itself is real and clean; the ordering is not. **This is a
   process defect in the window, not a defect in the environment.**
2. **It is a six-check report, not seven.** `org_topology` (Check 7) never ran — that is
   [[FD-PREFLIGHT-1]], confirmed live on this rig and fixed on its own branch. A re-run with the
   fix applied, same rig, same session, at 19:16:33Z returned seven checks with
   `org_topology: passed — 2 org(s), no staging seed orgs — prod-like single-tenant topology`, so
   the `clean_mirror` classification stands on its merits.

## Requirements of §1.12 that this window did NOT meet

- **Deploy rollback rehearsal was never run for TRAIN-5.** §1.12 requires it at T2. The only
  rehearsal artifact on this service is
  `docs/staging/evidence/fullsoak-2026-08/rollback-rehearsal-2026-08-21.md`, which belongs to the
  **chain-pair** window and a different image (`sha256:d7a956…`). Citing it here would be
  cross-window evidence reuse, which §1.11A forbids. Recorded as **not performed**.
- **No `public.staging_deploy_log` row exists.** The table does not exist on
  `gnkuaywlpmsaezwvlvhk` at all (`42P01` on `select * from public.staging_deploy_log`); the rig
  was deployed with a direct `gcloud run deploy`, and `scripts/staging/deploy.sh` is hard-scoped to
  the shared `arkova-worker-staging` service. The real, citable deploy artifact is the Cloud Run
  admin-activity audit entry for the `ReplaceService` call that created the revision:
  **`insertId 5ulxx4d2iqd`, 2026-08-21T18:39:17.127025Z, principal
  `270018525501-compute@developer.gserviceaccount.com`.**
- **Preflight ordering**, per above.
- **Six of ten members are not directly covered** — #2245 (not at all), #2258 (no HTTP surface),
  #2270 (indirect only), and the specific changed branches of #2251, #2266 and #2267.

None of these invalidate the clock, the coverage numbers, or the direct coverage that four members
do have. They are requirements that were not met, recorded as unmet.

## What this window does NOT cover

- **#2245's scope-minimisation change**, per above.
- **Any write-side surrogate handling** (#2267) — the sweep's whole point — because no
  surrogate-bearing write payload was driven.
- **Any quota-denial path** (#2251) — the driver org is unlimited.
- **Any quarantine / poison-record path** (#2266) — `processed: 0` on every cycle.
- **Mainnet economics.** The rig runs `network: signet`.
