# Wave 2 — T2 soak maturity record, closed 2026-08-21T03:51:57Z

**Rig:** `arkova-worker-wave2-2026-08-staging`, revision `00001-qir`, tag `train-w2`
**Supabase project:** `tkciooifwxwnkoizgalp` (`arkova-wave2-2026-08`, isolated)
**Declared tier:** T2 — 7 members
**Window:** 2026-08-20T15:51:57Z → 2026-08-21T03:51:57Z (**12.00 h**)

## Verdict

**The clock is satisfied. The load evidence is not sufficient for every member.**

Members whose changed behaviour this soak did **not** exercise must not be treated as
soak-covered on the strength of this window alone. Details in *Coverage gaps* below.

## Clock integrity — PASSES (FD-CLOCK-1)

Per FD-CLOCK-1 the clock is the serving revision's `creationTimestamp` plus integrity
conditions, not instance uptime.

| Condition | Observed |
|---|---|
| Serving revision | `arkova-worker-wave2-2026-08-staging-00001-qir` |
| Revision created | **2026-08-20T15:51:57.878019Z** |
| Revisions ever created on this service | **exactly one** — `00001-qir`. No redeploy occurred at any point. |
| Traffic | 100 % to `00001-qir` (tag `train-w2`) throughout |
| Ready condition | `True` |
| Health probes | 200 on every check across the window (3× per tick) |

The revision was never replaced, so the clock never reset. **12.00 h elapsed.**

## Load coverage — PARTIAL (38.2 %)

11 load cycles landed, each ~4,010 requests over a 25-minute run:

| Cycle start | Requests | 2xx (`cron`) |
|---|---|---|
| 17:04:50 → 17:29:50 | 4,010 | 25 |
| 18:02:26 → 18:27:26 | 4,006 | 25 |
| 18:55:15 → 19:20:15 | 4,011 | 25 |
| 20:45:40 → 21:10:40 | 4,009 | 25 |
| 21:39:51 → 22:04:51 | 4,011 | 25 |
| 22:35:07 → 23:00:07 | 4,011 | 25 |
| 23:30:25 → 23:55:25 | 4,009 | 25 |
| 00:25:36 → 00:50:36 | 4,010 | 25 |
| 01:20:56 → 01:45:56 | 4,009 | 25 |
| 02:16:13 → 02:41:13 | 4,006 | 25 |
| 03:07:21 → 03:32:21 | 4,009 | 25 |

**Total load time 4.58 h of a 12.00 h window — 38.2 %.** Gaps over 20 minutes:

| Gap | Duration | Cause |
|---|---|---|
| 15:51:57 → 17:04:50 | **73 min** | clock started before the load relauncher was wired |
| 17:29:50 → 18:02:26 | 33 min | inter-cycle idle |
| 18:27:26 → 18:55:15 | 28 min | inter-cycle idle |
| 19:20:15 → 20:45:40 | **85 min** | missed cycle |
| 21:10:40 → 21:39:51 | 29 min | inter-cycle idle |
| 22:04:51 → 22:35:07 | 30 min | inter-cycle idle |
| 23:00:07 → 23:30:25 | 30 min | inter-cycle idle |
| 23:55:25 → 00:25:36 | 30 min | inter-cycle idle |
| 00:50:36 → 01:20:56 | 30 min | inter-cycle idle |
| 01:45:56 → 02:16:13 | 30 min | inter-cycle idle |
| 02:41:13 → 03:07:21 | **26 min** | `ai.arkova.soak.wave-load` LaunchAgent failed to re-fire (last exit status 2); recovered via `launchctl kickstart` |

The ~30-minute gaps are structural: a 25-minute run on a 30-minute interval idles ~17 % by
design. The 73-minute, 85-minute and 26-minute gaps are genuine outages.

## Coverage gaps that bear on specific members

### `/api/v1` was DARK for the entire window

`switchboard_flags` on the rig does not contain `ENABLE_VERIFICATION_API`, so `get_flag`
fails closed and every `/api/v1/*` request returned **503 in under 10 ms — before reaching
application code**. Verified by querying the rig's `switchboard_flags` table directly (only
`ENABLE_ORG_CREDIT_ENFORCEMENT` and `ENABLE_PARTNER_PROVISIONING` are present).

This is **not** worker degradation — it is designed fail-closed behaviour, and clock
integrity is unaffected. But it means the harness's `reads` mode exercised the flag gate
and nothing behind it.

**No flag was written to the rig**, because writing to a soaking rig mid-window would taint
its own evidence.

### Members and whether this soak exercised them

| PR | Change | Exercised by this soak? |
|---|---|---|
| #2211 | ORG_ADMIN-gate the self-serve verification flow | **NO** — `/api/v1` dark |
| #2233 | stop reporting total ingestion failure as HTTP 200 | **NO / unverified** — response-status semantics on a dark API surface |
| #2245 | request minimal Drive OAuth scopes | Not covered — no OAuth flow driven |
| #2254 | count floor on pipeline linker-stall | Partially — cron path ran (275 × 200) |
| #2258 | scrub Sentry `event.extra` recursively | Not covered — no Sentry event forced |
| #2267 | surrogate-safe truncation for PostgREST | Indirectly — DB write paths exercised by cron |
| #2270 | gate Sentry cron check-ins to the prod service | Partially — cron path ran |

CLAUDE.md §1.12: *"Soak evidence must exercise the PR's changed behavior; generic synthetic
load is supporting worker-health evidence only, and if it does not cover the changed path,
the PR must include targeted staging/E2E evidence or an explicit Carson-approved
residual-risk exception."*

**#2211 and #2233 therefore require targeted evidence or an explicit residual-risk
exception before merge. They are not covered by this window.**

## 5xx accounting

**400 ERROR-severity entries in 12 h, all HTTP 503**, on synthetic-harness paths:
`/webhooks/microsoft-graph`, `/webhooks/checkr`, `/webhooks/adobe-sign`,
`/api/v1/webhooks/drive`, plus the `/api/v1` reads above.

The `/api/v1` subset is explained and verified (switchboard fail-closed). The `/webhooks/*`
subset is **consistent with** the same fail-closed pattern on a rig where those third-party
integrations are unconfigured, but **each was not individually verified** — stated as an
inference, not a measurement.

No container terminations, no OOM, no restarts, and no revision change across the window.

## What this window supports

**Supported:** the worker ran 12 h on a single unchanged revision at 100 % traffic with
health green throughout, absorbing ~44,000 synthetic requests across 11 cycles, including
275 authenticated cron requests all returning 200, with zero crashes or redeploys.

**Not supported:** any claim that `/api/v1` behaviour, the ORG_ADMIN verification gate, or
ingestion response-status semantics were exercised.

## Recommendation

1. Members #2245, #2254, #2258, #2267, #2270 — worker-health evidence stands; assess each
   on its own targeted tests.
2. **#2211 and #2233 — hold.** Either run targeted staging/E2E evidence against a rig with
   `ENABLE_VERIFICATION_API` present in `switchboard_flags`, or obtain an explicit
   Carson-approved residual-risk exception.
3. Before the next soak, seed `switchboard_flags` **at rig provisioning time**, so
   `/api/v1` is live from the clock start rather than dark for the whole window.
4. Add a watchdog for the load relauncher itself — the liveness gate watches *evidence* and
   had no way to notice that the agent producing it had stopped for 26 minutes.
