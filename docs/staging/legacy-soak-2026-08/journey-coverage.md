# SS4.3 journey-table coverage — legacy-soak-2026-08

Source table: [`PLAN-legacy-soak-pre-launch-window.md` §4.3](../../release/PLAN-legacy-soak-pre-launch-window.md#43-subsystem-to-journey-mapping--which-legacy-surfaces-get-which-exercises). Per the plan's own pre-mortem item #1 ("hollow soak"), every subsystem row below must have an assigned, scheduled exercise or be explicitly flagged NOT COVERED — a running clock alone is not evidence.

This pass was done 2026-07-28 by the RTE session that stood up `scripts/soak/loadgen.ts` and started it against both rigs. It records what the **durable load generator** (`scripts/soak/loadgen.ts`, running continuously against `arkova-worker-legacy-soak-2026-08-staging` for the 72h window) and the rig's own **Cloud Scheduler jobs** (5 jobs, `*/5` and `*/10`) actually exercise, vs. what still needs a dedicated one-off exercise this session did not run.

| Subsystem | Covering exercise | Status |
|---|---|---|
| **worker — auth/tenant boundary** | NONE from the load generator or scheduler jobs. The loadgen's `edge_no_apikey` action tests missing-auth (401/403), not cross-org header-trust (the actual defect class named in the plan: `x-org-id`/`req.params.orgId` trusted without membership check). | **NOT COVERED** — requires a dedicated repo-wide grep (`grep -rn "req.headers\['x-org-id'\]\|req.params.orgId"` across `services/worker/src/`) plus a live authenticated-org-A-vs-org-B exercise against every route the grep turns up. Not attempted this session; needs its own pass before this soak can be cited as having closed pre-mortem item #1 for this row. |
| **worker — SECURITY DEFINER RPC family** | NONE. The loadgen never calls RPCs directly (it only exercises the public HTTP API), and no `pg_proc`/`prosecdef` enumeration was run this session beyond the six already-revoked (0377) RPCs the launch soak's §5 covers. | **NOT COVERED** — needs `select proname from pg_proc where prosecdef and pronamespace='public'::regnamespace` cross-referenced against `anon`/`authenticated` grants, run once against `ryasykzdduzymschbucr`, then a positive+negative-path check for any newly-found over-broad grant. Not attempted this session. |
| **worker — credit/billing fail-open paths** | Partial, incidental only. The loadgen's `anchor_create_single`/`anchor_create_bulk` actions run continuously with `ENABLE_ORG_CREDIT_ENFORCEMENT=true` against a real `org_credits` row, so the **happy-path deduction accounting** gets exercised at volume. It does **not** perform the plan's actual ask: a targeted RPC-failure injection (breaking a specific credit-mutating RPC call path) to observe fail-open vs fail-closed behavior across the wider `creditLedger*`/`paymentTierRouter.ts` surface, only the one already-known `ai-extract.ts` path the runbook's failure-injection scenario #8 covers (credit-exhaustion-at-boundary, which this session DID verify live — see below). | **PARTIALLY COVERED** — boundary exhaustion (scenario #8 pattern) confirmed live this session: a fresh `org_credits` row was seeded at 0 balance and a real anchor create returned `402 insufficient_credits`, and a hardcoded sandbox `anchor_quota` gate (`anchorQuotaGate.ts`, `org_credits.anchor_quota`) was also found and hit live (402 `quota_exhausted` at count 10) before being raised to 50000 for this org so the loadgen's volume pillar isn't artificially capped. Neither of these is the plan's asked-for repo-wide fail-open grep + injection sweep — that remains open. |
| **worker — chain/broadcast/treasury** | Cloud Scheduler `recover-broadcasts` (`*/10`) runs continuously and is the mechanism failure-injection scenario #1 (GetBlock RPC outage) checks recovery through. No GetBlock-outage or mempool.space-outage fault was actually injected this session (no firewall rule / egress block was applied). The rig already runs on the mempool.space fallback provider (GetBlock has no valid signet credential — disclosed exception, see `smoke-gate-T0-2h.md`), so provider-outage injection against the *actual* configured provider (mempool.space) is what would be meaningful here, not a GetBlock test (GetBlock is already not in the live path). | **NOT COVERED** — no fault was injected this session. `recover-broadcasts` firing every 10 min for 72h is a monitor, not a failure-injection exercise; distinguish the two per the plan's own §0.9 language. WIF-vs-KMS precedence code review also not performed this session. |
| **frontend — client-side privacy boundary (§1.6)** | NONE — this is explicitly a static/code-review exercise per the plan's own text ("nothing to load-test if the boundary is sound"), not something the HTTP-level load generator can exercise. | **NOT COVERED** (by design — not a load-gen gap, a review-pass gap). Needs a targeted audit confirming no code path since baseline sends raw bytes/PII off-device, plus a run of the existing OCR fixture corpus (`src/lib/__fixtures__/ocr/*`). Not attempted this session. |
| **migrations — RLS + SECURITY DEFINER surface** | NONE — no adversarial cross-org/cross-role RLS pass was run against `ryasykzdduzymschbucr` this session beyond the org-scoped API-key isolation implicit in using a single seeded org (`5eed0000-…-b1`) for all loadgen traffic, which proves nothing about *other* orgs being denied since no second org was exercised. | **NOT COVERED** — needs a from-scratch pass per table attempting cross-org/cross-role access it should deny, not just a policy-exists check. A second synthetic org with its own API key would be needed to actually test the cross-org denial path; not created this session. |
| **edge** (`services/edge/`) | NONE. `scripts/soak/loadgen.ts` only targets the Cloud Run worker (`RIG_BASE_URL`), not `edge.arkova.ai` (the separate Cloudflare Worker MCP surface, `memory/project_mcp_edge_server.md`). | **NOT COVERED** — the plan calls this out as "essentially no review pressure... since before June"; this session doesn't change that. Needs its own tool/endpoint enumeration + auth/rate-limit verification + a basic volume pass, none of which this loadgen touches. |
| **integrations** (`bullhorn`, `clio`, `zapier`) | NONE. | **NOT COVERED** — plan itself scopes this to a code-review pass rather than a load exercise "given limited soak time"; consistent with that, no exercise was run this session. Flagging as not covered rather than silently treating the plan's own de-scoping as equivalent to "done." |

## What IS covered, and how, for the record

The load generator (`scripts/soak/loadgen.ts`, deployed as Cloud Run service `arkova-soak-loadgen-legacy-soak-2026-08`, `--no-cpu-throttling`, `min-instances=1`) runs continuously against `arkova-worker-legacy-soak-2026-08-staging` for the full window and exercises, generically (not the SS4.3-specific rows above):

- Sustained + periodic-burst HTTP load against `/api/v1/anchor/*` (lifecycle, single-create, bulk-create), `/api/v1/verify/*/proof`, `/api/v1/usage`, `/health` — this is **general worker-health/volume evidence per `CLAUDE.md` §1.12**, not a substitute for the SS4.3 rows' specific changed-path/legacy-surface exercises, which is exactly the distinction §1.12 itself draws ("generic synthetic load is supporting evidence only; it does not substitute for exercising the changed path").
- Edge-case slice: malformed fingerprint (expect 400), over-cap bulk batch >1000 rows (expect 400, no crash), no-API-key request (expect 401/403), duplicate-fingerprint idempotency (expect clean skip, no double charge) — run every cycle.
- Rate-limit path: confirmed live (`X-RateLimit-Limit: 1000`, keyed per API key) is real and enforced, not just present in code, incidentally exercising one column of the "worker — auth/tenant boundary" row's broader auth-middleware stack (but not the specific header-trust defect class named there).

## Bottom line

**2 of 8 rows have any real exercise this session (1 partial, 1 incidental-only); 6 of 8 are explicitly NOT COVERED.** The durable load generator closes the "is anything even running against these rigs" gap (previously true hollowness — no load generation was running at all before this session, see `smoke-gate-T0-2h.md` open item #4), but it does **not** close the SS4.3 pre-mortem item #1 gap on its own. The 6 NOT COVERED rows each need their own dedicated exercise (static review, adversarial multi-org RLS pass, RPC-grant enumeration, targeted fault injection, edge-surface pass) that a generic HTTP load generator structurally cannot perform. This should be treated as a punch list for the remainder of the 72h window, not as a soak-blocking finding by itself — but it must not be silently treated as satisfied by "the loadgen is running."

_Last refreshed: 2026-07-28 by RTE session — coverage claims verified against `scripts/soak/loadgen.ts`'s actual action set (read in full) and live `X-RateLimit-*` / `402` responses observed from the rig, not asserted from the plan's intent alone._
