# Chain-pair — T3 soak maturity record, clock closed 2026-08-21T16:51:23Z

**Rig:** `arkova-worker-fullsoak-2026-08-staging`, revision `00022-suy`, tag `cp0819rc`
**Image digest:** `sha256:d7a956079729b080a2e654f8014c9fbd270dbfc4392b1000e1a4e0630157ea45`
**Worker `git_sha` (from `/health`):** `daf3d6056788762ba56da5c328154e50fce03b59`
**Network:** `signet`
**Declared tier:** T3 — chain/treasury + anchor lifecycle + cron-on-anchors
**Revision clock start (FD-CLOCK-1):** 2026-08-19T16:51:23.324226Z
**Clock close:** 2026-08-21T16:51:23Z — **48.00 h, the full T3 minimum**

## Verdict

**Clock passes, load coverage is unbroken, and the trigger evidence is the most complete of
the 2026-08 windows.** 40,675 requests across the window with **zero gaps over 20 minutes**
(largest gap in the entire 48 hours: **42 seconds**)
and **six 5xx in total**, all six inside a single 84-second burst with an identified cause
and no recurrence in the following 15 hours. Triggers A, B and D all fired and are separately
evidenced; per-org isolation is evidenced. **Trigger C (fee ceiling) did not fire and is
recorded as un-exercised — it was not simulated.**

## Clock integrity — PASSES (FD-CLOCK-1)

| Condition | Observed |
|---|---|
| Serving revision | `arkova-worker-fullsoak-2026-08-staging-00022-suy` |
| Revision created | 2026-08-19T16:51:23.324226Z |
| Revision at close | **unchanged** |
| Revisions that served requests in-window | **`00022-suy` only** (3,000-request sample) |
| Traffic | **100 %**, tag `cp0819rc` |
| Container terminations / OOM | **none** |
| 5xx across the 48 h window | **6** (see below) |
| Health at close | **5/5 × HTTP 200** (0.303–1.282 s; the 1.28 s outlier is a cold-start on run 3, the other four 0.30–0.37 s) |

The clock is the serving revision's `creationTimestamp`, not instance uptime. `/health`
reports `uptime` around 79,000 s (~22 h) at close — that is the current *instance*, which
Cloud Run recycles, and it is **not** the soak clock. Reading it as the clock would understate
this window by more than a day.

## Load coverage — 100 % of elapsed clock, zero gaps over 20 minutes

Computed from Cloud Run request logs across the whole window (not from local driver logs),
pulled in 6-hour slices so no slice hit the 20,000-entry read cap:

| | |
|---|---|
| Requests in window | **40,675** |
| First request | 2026-08-19T16:51:27Z — **3.8 s** after clock start |
| Last request | 2026-08-21T16:51:22Z — **1 s** before clock close |
| Gaps > 20 min | **0** |
| **Largest gap in 48 h** | **0.7 min (42 s)** |
| Loaded coverage | **100.0 %** of the 48.00 h clock |
| Steady-state rate | ~513–530 req/h |

Captured at close: 2026-08-21T16:51:33Z. Hourly counts show no dead hour. Three bursts stand above the baseline — 3,621 at 20/21Z,
2,764 at 21/00Z and **10,879 at 21/01Z** (the Trigger B volume run).

This is a materially different coverage profile from the other 2026-08 legs: wave2 reached
38.2 % and wave3 58.1 %, both limited by `launchd` `StartInterval` scheduling from job *exit*.
The chain-pair leg submits anchors on every cycle and never went quiet.

## The six 5xx, with cause

All six are `503` on `POST /api/v1/anchor`, between 01:40:47Z and 01:42:11Z on 2026-08-21 —
an 84-second span, all from a single instance.

**Cause:** they are the ramp-up of the Trigger B volume run. In the surrounding window
(01:35Z–02:16Z) that instrument submitted **1,722 × 201 against 6 × 503 — a 0.35 % error
rate** — and from 01:46:43Z onward it sustained 8-way concurrent `201`s with no further
failures. Nothing else in the 48 h window returned 5xx.

**Not asserted:** that the 503s were harmless. They are a real rejection of six anchor
submissions during scale-up. What is asserted is that they were confined to one 84-second
ramp, self-recovered, and never recurred across the remaining ~15 hours of the window.

## T3 trigger coverage

| Trigger | Status | Evidence |
|---|---|---|
| **A** — size (10,000 pending) | **Fired 2026-08-21T01:30:00Z** | `trigger-a-fired-2026-08-21T0130Z.md` |
| **B** — age (3,000 + 3 h) | **Fired 2026-08-20T22:00:43Z** | `trigger-b-fired-2026-08-20T2200Z.md` |
| **C** — fee ceiling | **NOT EXERCISED** | see below |
| **D** — daily 03:00Z forced flush | **Fired, SECURED confirmed** | `trigger-d-fired-2026-08-21T0300Z.md` |
| Per-org isolation | **Verified** | `per-org-isolation-2026-08-20T2346Z.md` |

### Trigger C is un-exercised, and was not simulated

Trigger C fires on a Bitcoin **fee ceiling**. It cannot be driven on demand: it depends on
network fee conditions crossing a threshold, which no amount of submitted volume produces.
It remained un-exercised for the whole window, as it has for every prior window.

It would have been trivial to force a fee ceiling artificially and record "Trigger C fired."
That would be a fabricated control. The honest state is: **three of four batch triggers are
evidenced by this window; the fourth is not, and this record must not be cited as covering
it.**

## Environment basis — `fixture_seeded`, not `clean_mirror`, and why that is stated rather than rounded

The preflight for **this** soak ran 2026-08-19T16:21:26Z against `gnkuaywlpmsaezwvlvhk` and
returned **`environment_type = fixture_seeded`, 5 of 6 checks** — `submitted_anchors` failed
("Zero SUBMITTED anchors"). It is recorded here because a T3 maturity record that omits its
own preflight is incomplete.

**Do not confuse this with the rig's provisioning preflight.** The record in
`docs/staging/fullsoak-2026-08/isolated-rig-provision-fullsoak-2026-08.json` reads
`clean_mirror / exit 0 / 7-of-7 / 2026-08-11T16:29Z` — that is the **rig standup 8 days
earlier**, before the 7-day SOC2 window ran on the same project. Citing it for this window
would be stale-evidence reuse across soak windows. The 08-19 reading is the one that governs.

**Why `fixture_seeded` is an acceptable basis here:** CLAUDE.md §1.11A's `clean_mirror`-only
rule is written for **shared** staging (`ujtlwnoqfhtitcmsnrpq`). This is a founder-approved
**isolated** rig, and for isolated evidence §1.11A requires the preflight result be *named*,
not that it be `clean_mirror`. None of the three PRs in this union touches migrations, RLS or
schema; artifact rows were zero and the anchor corpus was real rather than fixture. The
soak-start doc reached this conclusion at clock start and explicitly declined to round it up.
It is recorded as `fixture_seeded` everywhere it appears.

## Requirements of §1.12 that this window did NOT meet

Stated here rather than left for a reader to discover:

- **Deploy rollback rehearsal was never run.** §1.12 requires it at T2 and T3. The only
  rehearsal artifact in the repo is `docs/staging/launch-72h-2026-08/rollback-rehearsal-0370-0377.json`,
  which belongs to a different soak. The RC manifest's `rollback_proof` of
  `N/A — no PR touches supabase/migrations/` covers **migration** rollback only; the deploy
  rehearsal is a separate requirement and was not performed. Prior revision `-00013-mrw`
  remains on the service as the untested rollback target. *Being closed post-close against the
  now-idle rig — see `rollback-rehearsal-2026-08-21.md` when it lands.*
- **No `public.staging_deploy_log` row is recoverable.** The rig was deployed with a direct
  `gcloud run deploy` on a digest-pinned image; `scripts/staging/deploy.sh` is hard-scoped to
  the shared `arkova-worker-staging` service and so never wrote a row. Cloud Build
  `325b791a-f0a0-4653-801e-596e4a467264` is the real deploy artifact and is cited in its place.
- **The soak's own CTO pre-mortem controls were never closed** — PM-A (treasury visibility
  across flush cycles), PM-B (`go.getblock.io` token-leak grep), PM-C (`[NODE-CRON]`
  missed-execution count) all remain `PENDING` in the RC manifest. *Being closed
  retrospectively from the window's logs.*

None of these invalidate the clock, the coverage, or the trigger evidence above. They are
requirements that were not met, recorded as unmet.

## What this window does NOT cover

- **Trigger C**, per above.
- **Cross-org behaviour under Trigger A.** All 10,000 anchors in the Trigger A run belong to
  a single org (`orgs = 1`). Per-org isolation is evidenced separately rather than by that run.
- **Mainnet economics.** The rig runs `network: signet`. Fee estimation, UTXO selection and
  broadcast behaviour under real mainnet fee pressure are not exercised by this window.
