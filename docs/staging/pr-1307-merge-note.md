# PR #1307 — merge note (RTE 2026-06-28)

Soak complete (48h+, 5k-revalidated). Migration 0347 applied to prod (ledger head 0347).
This doc-only file exists solely to present GitHub a fresh tree so it recomputes a wedged
phantom `mergeable=CONFLICTING` (real 3-way merge vs main is clean). Doc-only; soak unaffected.
