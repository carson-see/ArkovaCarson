# Day-0 Behavioral Flag Probes — 7-Day Full-Functionality Soak (BL-3)

**Executed:** 2026-08-12, 13:45–14:13 UTC ·
**Rig:** Cloud Run `arkova-worker-fullsoak-2026-08-staging` rev `00012-f45` (100% traffic), project `arkova1`, us-central1 ·
**Rig `/health`:** `healthy`, `git_sha f5d1070fcca2027fd7ab56a596d8e1ae27ae4a58`, signet · worker boot 13:32:32Z (boot log) ·
**DB:** Supabase `gnkuaywlpmsaezwvlvhk` · **Probe method:** premortem BL-3 PASS criterion 3 — every verdict is a **named row-count delta**, never an HTTP 200. Baseline snapshot taken 13:45:14Z (all probe tables at 0 except `org_credit_deductions=7`, `job_queue=4`, `anchors=5`, `api_keys=4`, `audit_events=102`, `treasury_alert_state=1`).

**Constitution compliance:** zero direct writes to `anchors` / `anchor_proofs`; zero writes to `treasury_cache`; no status values written anywhere; every anchor and attestation created via the real product API; no deploy, env change, Cloud Scheduler change, or restart (soak clock untouched — worker uptime continuous since 13:32Z). All code-path claims verified against the rig's exact SHA `f5d1070fc` (present in local repo; key worker files diff-identical).

**Tally: 15 PASS · 1 FAIL (real defect found) · 4 PARTIAL · 5 NOT RUN (each with rationale).**

---

## 1. Probe results

Auth notes (redacted): user JWTs minted via `POST <supabase>/auth/v1/token?grant_type=password` for `sarah@arkova.ai` (org A = Arkova `aaaaaaaa-…0001`) and `demo-admin@arkova.local` (org B = Acme `bbbbbbbb-…0001`), seeded password per `supabase/seed.sql`. Cloud Run IAM token sent via `X-Serverless-Authorization` so the app-level `Authorization` header can carry the Supabase JWT (the standard header is consumed by Cloud Run IAM — the flag-seed plan's curl recipe only works for routes that don't read `Authorization`). API keys minted via real flow `POST /api/v1/keys` (org A `ak_live_3b66…`, org B `ak_live_4847…`, org B webhooks-scope `ak_live_eb1f…`). Cron routes authed with `X-Cron-Secret` (secret `cron-secret`, redacted).

| # | Flag | Trigger (tokens redacted) | count_before | count_after | Delta / evidence | Verdict |
|---|---|---|---|---|---|---|
| 1 | `ENABLE_AI_EXTRACTION` | JWT-B `POST /api/v1/ai/extract` (strippedText+credentialType+fingerprint) 13:49:00Z | `ai_usage_events` = 0 | 1 | +1 `extraction/success=true` @13:49:04Z, `provider:"gemini"` (real model call, confidence 0.76) | **PASS** |
| 2 | `ENABLE_SEMANTIC_SEARCH` | JWT-A `POST /api/v1/ai/embed/batch` (4 org-A anchorIds) 13:51:45Z, then `GET /api/v1/ai/search?q=…` 13:51:46Z | `credential_embeddings` = 0 | 4 | +4 embeddings (`succeeded:4`); search executed: +1 `ai_usage_events` row (1 credit, 13:51:46Z; endpoint logs `event_type='embedding'`, there is no `'search'` event type). Search returned 0 matches at 0.7 threshold — the 4 seed anchors carry empty metadata; delta assertions hold regardless | **PASS** |
| 3 | `ENABLE_AI_FRAUD` | JWT-A `POST /api/v1/ai/integrity/compute` {anchorId d2ec2b2d…} 13:52:46Z | `integrity_scores` = 0 | 1 | +1 row (anchor d2ec2b2d, overall_score 38, FLAGGED, computed_at 13:52:46.81Z). NB: premortem §5.1's "computeIntegrityScore has no caller" is stale at this SHA — the route exists and works | **PASS** |
| 4 | `ENABLE_AI_REPORTS` | JWT-B `POST /api/v1/ai/reports` {credential_analytics} 13:52:57Z | `ai_reports` = 0 | 1 | +1 row d5329bc0…, background generation reached `COMPLETE` | **PASS** |
| 5 | `ENABLE_VERIFICATION_API` | API-key-A `GET /api/v1/verify/ARK-2026-BAC1FC13` 13:58:43Z | `audit_events(VERIFICATION_QUERIED)` = 0 | 1 | +1 audit row (target ARK-2026-BAC1FC13 @13:58:43.61Z). **Plan corrections:** the route takes `:publicId`, NOT a fingerprint (fingerprint → 404), and it writes `audit_events`, NOT `verification_events` (that table stayed 0 and is not written by this route at this SHA) | **PASS** |
| 6 | `ENABLE_WEBHOOK_HMAC` | Pair to `POST /webhooks/docusign` 13:55:43Z: forged sig (HMAC with wrong key), then valid sig (HMAC-SHA256-base64 of the raw body under the fixture HMAC secret; header `X-DocuSign-Signature-1`) | `docusign_webhook_nonces` = 0 | 1 | Forged leg: **401 `invalid_signature`, nonce delta = 0** (the zero IS the assertion). Signed leg: 202, nonce +1 (envelope `fullsoak-day0-env-001` @13:55:44Z). NB: route is `/webhooks/docusign`, not `/api/v1/webhooks/docusign` as in the plan | **PASS** |
| 7 | `ENABLE_DOCUSIGN_WEBHOOK` | Signed synthetic Connect `envelope-completed` (same call as #6 signed leg) | nonces 0 / `organization_rule_events` 0 / ds jobs 0 | 1 / 1 / 1 | nonce +1; rule event +1 (`ESIGN_COMPLETED`, vendor docusign, PENDING); `job_queue` +1 (`docusign.envelope_completed`, pending) | **PASS** |
| 8 | `ENABLE_CONNECTOR_ARTIFACT_ENQUEUE` | Cron `POST /jobs/docusign-envelope-completed` 13:56:16Z | job attempts 0 | attempts 1, status `failed` | `{claimed:1, failed:1}`; `last_error='docusign_integration_missing_base_uri'` (bounded token, no payload bytes — §1.6A clean). `connector_artifact` unchanged (0): the flag-gated enqueue sits **after** the vendor fetch, which requires a real DocuSign OAuth tenant this rig cannot have. Claim/execute path proven; fetch→artifact leg structurally untestable here | **PARTIAL** (declared untestable leg) |
| 9 | `ENABLE_CONNECTOR_ARTIFACT_DRAIN` | Fixture artifact via **production RPC** `enqueue_connector_artifact` (14:02:47Z), drained by the rig's scheduled pass @14:03:01–03Z; my forced `POST /jobs/drain-connector-artifacts` 14:03:04Z confirmed idempotency (`claimed:0` after) | artifact `pending`; anchors 9; deductions 11 | artifact `anchored`; anchors 10; deductions 12 | Full named delta: `connector_artifact` pending→anchored (anchor_id e1737709), `anchors` +1 (**ARK-DOC-S3DQE5**, org B, fingerprint = the sha256 the signed webhook carried, SUBMITTED with real txid within ~90 s), `org_credit_deductions` +1 (`anchor.secure` @14:03:01Z) | **PASS** |
| 10 | `ENABLE_ORG_CREDIT_ENFORCEMENT` | Anchor submissions + drain (no dedicated trigger needed) | `org_credit_deductions` = 7 | 14 | +6 `anchor.create` (API submissions, −1 each) and +1 `anchor.secure` (connector drain). Debit fires on both the API-create and drain-secure paths | **PASS** |
| 11 | `ENABLE_RULES_ENGINE` | Rule event from #7 + rules fixture; processed by the rig's scheduled pass @13:59:01Z; my forced `POST /jobs/rules-engine` 14:01:21Z → `events_processed:0` (idempotent — nothing left) | event `PENDING`; executions 0 | event `PROCESSED`; executions 1 | `organization_rule_events` PENDING→CLAIMED(13:59:01)→PROCESSED(13:59:02); `organization_rule_executions` +1 (rule c783d0b6, PENDING) | **PASS** |
| 12 | `ENABLE_RULE_ACTION_DISPATCHER` | Forced `POST /jobs/rule-action-dispatcher` 14:01:53Z | execution 6c2d0cd1 `PENDING` | `SUCCEEDED` | `{dispatched:1, succeeded:1}`; output_payload = routed QUEUE_FOR_REVIEW marker, completed_at 14:01:54Z | **PASS** |
| 13 | `ENABLE_QUEUE_REMINDERS` | Forced `POST /jobs/queue-reminders` 14:01:54Z (every-minute cron rule fixture) | executions 1 | 2 | `{rules_evaluated:1, reminders_scheduled:1}`; +1 execution row 22c2d8d0 (rule d1e6618c) @14:01:54Z | **PASS** |
| 14 | `ENABLE_EXPIRY_ALERTS` | Forced `POST /jobs/check-credential-expiry` 14:07:25Z | — | — | **HTTP 500 `Query failed`.** Flag resolution IS proven ON twice over: boot log `ENABLE_EXPIRY_ALERTS {source:'db', value:true}` on rev 00012-f45, and the handler did NOT take its `skipped` branch. The handler then queries `anchors.not_after` and `anchors.document_title` — **neither column exists** (schema has `expires_at`; no title column). Rig log 14:07:25.573Z: `Failed to query expiring credentials — column anchors.document_title does not exist`. Rig schema = prod ledger head parity, so **this job 500s in prod too whenever the flag is on**. The plan's delta (`webhook_delivery_logs +N for compliance.document_expiring`) is doubly unreachable: `compliance.document_expiring` is not even a registrable event type (see #16) | **FAIL — real defect (code/schema mismatch in expiry-checker)** |
| 15 | `ENABLE_TREASURY_ALERTS` | Forced `POST /jobs/treasury-alert-check` 14:07:25Z — **no treasury_cache fixture, per anti-fabrication rule** | `treasury_alert_state.updated_at` 2026-08-11T16:02:09Z | unchanged | 200 `{fired:false, reason:"Balance above threshold", below_threshold:false, price_unknown:false}` — decision path executed against the real cache. Below-threshold decision path untestable without a cache fixture; **declared partial**. Observation: the state row (below_threshold=true from 08-11) was NOT updated by an above-threshold pass — state writes appear transition-gated; delivery legs unbound on this rig | **PARTIAL** (by declaration) |
| 16 | `ENABLE_OUTBOUND_WEBHOOKS` | Endpoint via real API (`POST /api/v1/webhooks`, webhooks:manage key) then real test delivery `POST /api/v1/webhooks/self-service/<id>/test` ×3, 14:06:29–55Z | `webhook_endpoints` 0; `webhook_delivery_logs` 0 | 1; 3 | 3 real outbound HMAC-signed POSTs logged: 2 failed (httpbin.org 503 — sink flaky, attempts logged correctly) + 1 success (postman-echo 200). Registrable event types are only anchor/credential lifecycle — `compliance.document_expiring` rejected by the create schema (feeds finding #14). Endpoint e9b82469 left ACTIVE (subscribed anchor.submitted/secured/batch_secured) so real lifecycle deliveries accrue during the week | **PASS** |
| 17 | `ENABLE_DRIVE_WEBHOOK` | Forged-token then valid-token `POST /api/v1/webhooks/drive` (X-Goog-Channel-ID/-Token/-Message-Number) 14:10:28–29Z | `drive_webhook_nonces` = 0 | 1 | Forged: **401 `invalid_channel_token`, delta 0** (rig log confirms rejection). Valid: 200, nonce +1 (channel f3ebf05d, msg 2, 14:10:29Z). **Plan correction:** channel resolution is `org_integrations.subscription_id` + token in `account_label` JSON — there is no `drive_watch_state` table at this SHA | **PASS** |
| 18 | `ENABLE_DRIVE_CHANGES_RUNNER` | Same valid push (runner runs after nonce insert) | — | — | Runner entered and gracefully skipped: rig log 14:10:30.957Z `drive runner: integration has no last_page_token — skipping`. The changes.list vendor leg requires real Google OAuth + page token — the plan itself says count the claim, not completion | **PARTIAL** (declared untestable leg) |
| 19 | `ENABLE_PARTNER_PROVISIONING` | `POST /api/partner-provisioning/requests` + `GET /api/partner-provisioning` (JWT-B) 13:56:56Z | `audit_events` | unchanged | Both 404. **The surface is routeless at this SHA by design** — `index.ts:545-555` (verified at `f5d1070fc`): "No routes are mounted in this slice… While dark or routeless, every request here 404s." The flag row is seeded true and the gate is mounted, but flag-on and flag-off are indistinguishable at the HTTP layer (both 404) and no `audit_events` delta is possible. The plan's probe row (request→approve→provision) is unexecutable against this build. This is the HakiChain onboarding path — it remains **declared untested** for this soak | **PARTIAL / UNTESTABLE** (plan-vs-code discrepancy) |
| 20 | `ENABLE_ATTESTATION_ANCHORING` | Attestation via real API `POST /api/v1/attestations` (JWT-B, 201 @14:13:04Z) then forced `POST /jobs/anchor-attestations` 14:13:14Z | attestation `PENDING` | `ACTIVE` | `{processed:1, batchId att_batch_…, merkleRoot 6b73c273…}` → ARK-ACC-AUD-095274 status ACTIVE with real signet `chain_tx_id 0cf45652eebf…` @14:13:15Z | **PASS** |
| 21 | `org-queue-scheduler` (DEG-5 fix proof) | Rig's own scheduled pass 13:59:03–05Z + my forced `POST /jobs/org-queue-scheduler` 13:59:32Z (200, `claimed:0` — orgs just ran, correct idempotency) | `organization_queue_runs` = 0 | 2 | +2 rows, trigger `scheduled`, **status `succeeded`**, processed_count 2+2, real batch txids `eb28b03a…` (org A) / `d73a3f0b…` (org B). DEG-5 (INTERNAL/13 every 5 min, zero rows ever written) is **fixed behaviorally on this revision**: the job now completes and writes run rows | **PASS** |
| 22 | `FORCE_DYNAMIC_FEE_ESTIMATION` | Boot log of rev 00012-f45 (read-only `gcloud logging read`) | prior revs: `fee=Static` | — | 13:32:39.921Z `Bitcoin chain client initialized … feeEstimator: "Mempool.space", provider: "Mempool.space REST API", signer: "WIF (ECPair)"` — the dynamic fee path (BL-2/DEG-2 fix) is live; the 3 real broadcasts above rode it. Week-long fee-rate variance across `anchor_txid_journal` (6 rows now) remains a soak-window observation | **PASS** (boot-truth leg) |
| 23 | `USE_MOCKS` inverse / mock detector | `SELECT count(*) FROM anchors WHERE chain_block_height > 400000` @14:12:03Z | 0 | 0 | Zero mock-heighted anchors after all probe activity (MockChainClient seeds ~800000; signet tip ~317k) | **PASS** (must stay 0 all week) |
| 24 | `ENABLE_FRAUD_DETECTION` | none | — | — | No worker consumer exists (UI-only per the plan's own table) — no server probe claimed | **NOT RUN** (by design) |
| 25 | `ENABLE_BATCH_ANCHORING` (forced flush) | reserved for main session | — | — | NOT RUN per tasking (`/jobs/batch-anchors?force=true` untouched). Supporting evidence already on this revision: the 13:59Z org-queue pass and 14:03Z drain pass each drove PENDING→SUBMITTED with real txids + `anchor_txid_journal` rows (now 6) | **NOT RUN** (main session) |
| 26 | `ENABLE_PUBLIC_RECORD_*` | deferred | `public_records` = 0 | 0 | Deliberately not probed on Day 0: `fetch-federal-register` hits a live external API and `anchor-public-records` (bound every 10 min on this rig, currently no-oping on an empty table) would convert an unbounded fetch batch into PENDING anchors, contaminating the controlled Day-0 anchor cohort. Probe later in the window under an explicit record limit if coverage is wanted | **NOT RUN** (scope decision, documented) |
| 27 | `ENABLE_CONFIRMATION_PROOF_BACKFILL` | scheduler-driven (`populate-confirmation-proofs`) | — | — | Covered by the main session's chain end-to-end (BL-2 PASS criterion 3: 80-raw-byte `block_header`) | **NOT RUN** (main session) |
| 28 | `ENABLE_ALLOCATION_ROLLOVER` / `ENABLE_CE_KEY_EXPIRY_ALERTS` | none | — | — | Pinned OFF on the revision by design (plan §a); OFF rationale recorded there; no probe warranted | **NOT RUN** (OFF by design) |

---

## 2. Findings (beyond the verdicts)

1. **F-D0-1 (defect, prod-relevant): `check-credential-expiry` 500s on every run** — handler selects `anchors.not_after` and `anchors.document_title`; the schema (rig = prod ledger parity) has `expires_at` and no title column. Every scheduled/forced run fails after the flag gate. Compounding: its delivery event `compliance.document_expiring` is not a registrable `webhook_endpoints` event type, so even a fixed query has no subscriber path. ENABLE_EXPIRY_ALERTS is effectively dead end-to-end at this SHA. Needs a code fix (post-window unless founder re-pins) + bug-tracker entry.
2. **F-D0-2 (rig topology, affects runbook): PENDING anchors cannot sit on this rig while an org is org-queue-due.** The scheduled `org-queue-scheduler` (`4-59/5 * * * *`) claimed both orgs and force-flushed my first 4 API anchors within ~107 s of creation (13:59:03Z pass; that flush IS probe #21's PASS evidence). Orgs are now not-due until ~2026-08-13 13:59Z, so the fresh PENDING cohort (below) holds until then; the daily forced flush is `0 3 * * *` UTC (re-pointed to prod cadence — DEG-1 mitigation visible live). Main session: run the observed forced flush **before 03:00Z** and before the orgs come due again.
3. **F-D0-3 (plan corrections for the flag-seed table):** verify route is `/api/v1/verify/:publicId` (not fingerprint) and its delta is `audit_events(VERIFICATION_QUERIED)` (not `verification_events`); DocuSign webhook path is `/webhooks/docusign` (not `/api/v1/...`); Drive channel state lives in `org_integrations.subscription_id`/`account_label` (no `drive_watch_state` table); search usage logs as `event_type='embedding'`.
4. **F-D0-4 (observation): AI-credit gating is inconsistent.** With `ai_credits` empty, `/ai/embed` and `/ai/search` fail closed (402), but `/ai/extract` proceeded and returned `creditsRemaining:null` — extraction is effectively un-gated when no allocation row exists. Worth a ticket; not fixed here.
5. **F-D0-5 (stale premortem claim):** §5.1 "AI fraud scoring: `computeIntegrityScore` has no caller" is false at this SHA — `/api/v1/ai/integrity/compute` calls it and it works (probe #3).
6. **Auth mechanics for future probes:** app-level auth on the rig requires `X-Serverless-Authorization: Bearer <gcloud identity token>` + `Authorization: Bearer <supabase JWT>` (Cloud Run consumes the former, the app reads the latter).

## 3. Fixture ledger (every row created, per §1.11A honesty rules)

| Table | Row(s) | Why | Created |
|---|---|---|---|
| `ai_credits` | ae991e38 (org A), 63d973a4 (org B) — 10,000 monthly allocation, period 2026-08 | AI embed/search fail closed with no allocation row; no billing flow provisions AI credits on the rig | 13:51Z |
| `org_integrations` | 517ef83f — org B, provider docusign, account `fullsoak-day0-probe-acct`, per-org `hmac_keys` (synthetic key, redacted), no OAuth tokens | DocuSign webhook + HMAC probes; fetch leg expected to fail (documented) | 13:54Z |
| `org_integrations` | bf6ddc8c — org A, provider google_drive, channel `f3ebf05d-…`, channel_token in account_label (redacted), no OAuth tokens | Drive webhook probes; changes-runner leg expected to skip | 14:09Z |
| `organization_rules` | c783d0b6 (ESIGN_COMPLETED, vendors [docusign], QUEUE_FOR_REVIEW), d1e6618c (SCHEDULED_CRON `* * * * *`, QUEUE_FOR_REVIEW) — org B | rules-engine / dispatcher / queue-reminders probes; pure marker action, no external side effect | 13:55Z |
| `connector_artifact` | c81421fa — via **production RPC** `enqueue_connector_artifact`, org B, external_ref = the webhook's envelope id, fingerprint = the webhook's document sha256 | stands in for the vendor-fetch result that is structurally unreachable (no real DocuSign tenant); consumed by the drain into anchor ARK-DOC-S3DQE5 | 14:02Z |
| `webhook_endpoints` | e9b82469 — via real API, sink now `https://postman-echo.com/post`, events anchor.submitted/secured/batch_secured, ACTIVE | outbound-webhook probe; left active so soak-week lifecycle events produce delivery evidence (payloads carry public_id/status only — no PII) | 14:06Z |
| `api_keys` | 3 keys via real `POST /api/v1/keys` (org A anchor/verify scopes, org B anchor/verify scopes, org B webhooks:manage) — raw keys held only in the operator scratchpad | anchor-submit requires an API key; webhook CRUD requires webhooks:manage | 13:47–14:05Z |
| `attestations` | ARK-ACC-AUD-095274 — via real API, then anchored by the real job (real signet tx 0cf45652…) | attestation-anchoring probe | 14:13Z |

Anchors/attestations created (all via real API flows, never SQL): 4 API anchors 13:57Z (auto-flushed 13:59Z — see F-D0-2), 1 drain anchor ARK-DOC-S3DQE5 14:03Z (SUBMITTED), 1 attestation 14:13Z (ACTIVE), 2 PENDING cohort anchors 14:11Z (below). Signet broadcasts attributable to probe activity: `eb28b03a…`, `d73a3f0b…` (org-queue flush batches), drain batch txid on ARK-DOC-S3DQE5, `0cf45652…` (attestation) — all on the frozen revision's dynamic-fee path, mainnet untouched.

## 4. Handed to the main session

- **PENDING anchors (unflushed, per tasking):** `ARK-2026-2432CB45` (org A `aaaaaaaa-…0001`, fp `61d23258…`), `ARK-2026-9476A947` (org B `bbbbbbbb-…0001`, fp `d403127d…`), created 14:11:50–51Z, confirmed still PENDING at 14:12:03Z. They survive until the 03:00 UTC daily forced flush or until the orgs come org-queue-due again (~2026-08-13 13:59Z) — run the observed forced-flush test before either.
- The earlier 4 API anchors + drain anchor + prior 5 baseline anchors are all SUBMITTED with real txids awaiting confirmation — inputs for BL-2 close-out (two-explorer confirmation + 80-byte `block_header` proof).
- `SOAK_GATE_DISABLED` flip and clock start remain sequenced per premortem §6.3 (not touched here).

_Every count above was read live from `gnkuaywlpmsaezwvlvhk` via bounded SQL between 13:45 and 14:14 UTC on 2026-08-12; log lines via read-only `gcloud logging read` against `arkova1`. No infra was modified._
