# Post-prep wave plan — 27 prepared PRs (2026-08-20)

**Every open non-dependabot PR has been rebased onto `main` @ `b6cfad73c`, re-gated, and had
its evidence block repaired. Heads were verified against GitHub individually, not taken from
agent reports. This document proposes how they soak. Nothing above T0 is readied without
Carson's sign-off.**

## Rig availability is the binding constraint

| Rig | Occupied by | Frees |
|---|---|---|
| fullsoak (`gnkuaywlpmsaezwvlvhk`) | chain-pair + rate-limit union `daf3d6056` (#2216/#2250/#2269) | 2026-08-21T16:51Z |
| standing (`fizyjojbebyalirtjjht`) | migration-T3 union `3baf16015` (#2219/#2235/#2248) | 2026-08-22T14:00Z |

Two rigs, 27 PRs, and §1.12 minimums of 2h/12h/48h. Soaking each PR alone is ~3 weeks of
wall clock. The union pattern already proved out twice this week (chain-pair carried three
PRs; migration carries three more), so the plan below unions by **interaction safety**, not
by convenience — every union member's changed behavior still needs its own targeted driver,
per §1.12's rule that generic load is supporting evidence only.

## Wave 0 — merges on CI alone, no soak (now)

| PR | Tier | Why no soak |
|---|---|---|
| #2285 | T0 | Docs only (STAGING_RIG.md + rebuild record). Gcloud-policy guard satisfied with reasoned allow-markers. |
| #2271 | T1 | **Already deployed to production and verified live for >24h** via Vercel branch-promote; evidence block carries the live-bundle verification. Merging only makes `main` match what prod already serves — leaving it unmerged is the actual risk. |
| #2221 | T0 | RC manifest; merges with its bound PR. |

## Wave 1 — frontend/config T1 union (2h, first rig to free)

#2241 → #2255 → #2256 (landing order; the test-file collision is already resolved by
#2255's rename), plus #2275, #2215, #2259.

All frontend, seed-fixture, or declaration-only. No worker runtime surface, no schema.
Targeted evidence per PR: record not-found render timing, RLS-denied rename failing loudly,
the records-surface dedup, invite pending/resend visibility, RFC-9562 fixture UUIDs, and
alert-rule environment scoping (declaration-only — must NOT claim the rules are live in
Sentry; that is a manual admin step).

## Wave 2 — worker T2 union A (12h)

#2258 + #2270 (three-way sentry co-merge already verified clean, `PROD_SERVICE_NAME`
appears exactly once in the merged tree), #2254, #2233, #2267, #2245, #2211.

Union rationale: disjoint files, and the two sentry PRs were explicitly co-merge-tested.
Targeted drivers: recursive `event.extra` scrub with a real nested payload; cron check-ins
firing ONLY for the prod service name (and the escape hatch working); both alert floors
under a real ANALYZE cycle; ingestion routes surfacing 502/503/207 instead of blanket 200;
astral-text truncation at webhook/error/registry boundaries; Drive scope minimization with
no consumer of a dropped scope; the ORG_ADMIN gate with self-serve VERIFIED preserved.

## Wave 3 — worker T2 union B (12h)

#2272 + #2276 (digests), #2232, #2246, #2220 (+#2221), #2252 (which supersedes #2247 on
merge), #2274.

Note the two digests have DIFFERENT models and the soak must not conflate them: #2272 is
per-org and skips empty queues; #2276 is a platform-wide snapshot that always sends. Both
must prove role-resolved recipients, opt-out honored, and no cross-user PII or fingerprints
in a body.

**#2230 → #2236 land in this wave in that order** — #2236 is stacked on #2230's branch and
auto-retargets when #2230 merges and its branch is deleted. Do not retarget manually (it
drops CI).

## Wave 4 — T3 (48h, needs a rig to itself or a clean-mirror union)

#2249 (anchor-lifecycle path), #2266 (anchor-adjacent quarantine), #2251 (concurrency /
data-integrity, self-declared above its path floor).

#2266 and #2267 share a byte-identical `utf16-truncate` utility (content-hash verified), so
if #2267 lands in Wave 2 the utility is already on `main` and #2266 reduces to its
quarantine half. #2251 was merge-tree-checked against the soaking rate-limit branch: clean.

## Decisions this plan needs from Carson

1. **Approve the wave grouping and order**, or amend it.
2. **§1.12 exception for the SDK PRs (#2252, #2274, #2247).** SDK paths force T2 with no
   override label, but an SDK-only PR deploys nothing — there is no runtime surface to soak.
   Either grant the exception or tell us to soak them as riders inside a worker union purely
   to satisfy the gate (which would be soak theatre, and this document does not recommend it).
3. **Dependabot (6 PRs, all held `do-not-merge`).** Landing order when you want them:
   #2262→#2228, #2263→review→#2264 (**the KMS 6.0 major touches the dormant GCP signing path
   and a hand-rolled type shim — that one needs real review, not a routine bump**),
   #2260→#2261, and #2279 (nanoid, low risk).

## What is NOT asserted

No PR in Waves 1-4 has soaked. Tiers are the live detector's output, re-verified per PR
during prep (five were wrong and were corrected UPWARD: #2230 T1→T2, #2249 T2→T3,
#2270 T1→T2, #2259 T0→T1, plus #2232 which had no evidence heading at all). The
Staging Soak Evidence Gate is RED on every prepared PR by design — the blocks say
`NOT RUN — soak pending`, and making that gate green today would require fabricating a
result or engaging the bypass. The red gate is the honest signal.
