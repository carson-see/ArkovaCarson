# agents.md — lib

_Last updated: 2026-08-03_

## 2026-08-03 CTDL data link copy (Lane 3, CE demo-gap bug blitz)

`copy.ts` gained `CTDL_DATA_LINK_LABELS` (placed right after `SOURCE_PROVENANCE_LABELS`) — the public verify page's new "Structured data: CTDL data (JSON-LD)" link to the existing `GET /api/v1/credentials/:publicId/ctdl` projection (see `src/components/verification/CtdlDataLink.tsx` + that folder's `agents.md`). Copy is a data-FORMAT description only, never a Registry-membership/publication-status claim — scanned clean by `copy-claims-gate.test.ts`'s whole-file `PROHIBITED_CLAIM_PATTERNS` sweep (imports the same patterns from `services/worker/src/ctdl/ctdl-claims-guard.ts`).

## 2026-07-28 R19 — fingerprint_source evidence class (advances SCRUM-2481)

New `fingerprintSource.ts` (mirrors `sourceProvenance.ts`) parses the `fingerprint_source` anchor field: `document_bytes` (a real file's bytes were fingerprinted client-side, §1.6) vs `issuer_record_attestation` (no source document was supplied — the CSV bulk-import row-mode path in `csvParser.ts` synthesizes the fingerprint from row text via `buildRowCanonical`/`sha256Hex` when no fingerprint column is mapped). This is an axis ORTHOGONAL to `verification_level`/`EvidenceLevel` in `sourceProvenance.ts` (that classifies credential-SOURCE-IMPORT authentication tiers, e.g. Credly/Accredible/URL-capture) — do not conflate the two or add `fingerprint_source` values to the `EvidenceLevel` union.

`csvParser.ts`: `BulkAnchorRecord` gained `fingerprintProvided: boolean` — the structural signal (was a fingerprint CSV column mapped) set by `extractAnchorRecords`/`extractAnchorRecordsAsync` at the SAME moment the row-text-hash-vs-supplied-hash branch decision is made. This is sent to `bulk_create_anchors` (migration `0376`), which computes `fingerprint_source` SERVER-SIDE from this boolean — never trust a client-supplied evidence-class label (that pattern is the exact SCRUM-2481 P0 gap this closes for this one field).

`validators.ts` `AnchorCreateSchema` gained `fingerprint_source: z.enum(['document_bytes','issuer_record_attestation']).optional().nullable()` — set to `'document_bytes'` by `SecureDocumentDialog.tsx` (the single-document Secure Document flow always hashes real file bytes).

New `copy.ts` block: `FingerprintSource` type, `FINGERPRINT_SOURCE_LABELS`/`_DESCRIPTIONS`/`_TRIAD` (measured/asserted/NOT-asserted per §1.5), `RECORD_ATTESTATION_LABELS` (the row-import issuer-attestation checkbox copy in `BulkUploadWizard.tsx`). Purely additive — does not touch `EVIDENCE_LEVEL_*`.

**R-7 claims gate invariant:** `issuer_record_attestation` copy must never state or imply Arkova received, reviewed, or verified a source document. Enforced by `fingerprintSource.test.ts` + `FingerprintSourceDisplay.test.tsx`.
## 2026-07-28 W2 / F1 spreadsheet dual-mode document extraction (founder ruling, PR TBD)

`ocrWorker.ts` gained a new `extractTextFromSpreadsheet()` handler (dynamic `import('xlsx')`, the SheetJS package, added as a new pinned dependency `xlsx@0.18.5`) routed for `.xlsx`/`.xls`/`.ods` (by MIME OR extension, same dispatch pattern as the existing PDF/DOCX branches). Each sheet in the workbook is rendered via `XLSX.utils.sheet_to_csv` and joined into one text block; `OCRResult.method` gained a `'spreadsheet'` member. `.csv` is deliberately left on the pre-existing `TEXT_TYPES`/`TEXT_EXTENSIONS` plain-text path — it was never broken. Before this change, `.xlsx`/`.xls`/`.ods` fell through every branch in `extractText` and hit the generic `UNSUPPORTED_FILE_TYPE` throw — a silent soft-fail (the F1 sprint amendment specifically bans soft-fail-to-manual-entry for the 22 launch formats). New fixtures `src/lib/fixtures/spreadsheets/sample-roster.{xlsx,xls,ods,csv}` are REAL binary files generated with the `xlsx` package's own writer (see that folder's `agents.md`), consumed by `ocrWorker.test.ts` and `xlsxParser.test.ts` for genuine (non-mocked) round-trip extraction tests. Row-mode (`xlsxParser.ts`, backed by `read-excel-file`, powering per-record bulk/credential-issuance import) is completely untouched — this is a NEW, separate document-mode path, not a replacement.

The actual mode CHOICE lives in `src/components/anchor/FileUpload.tsx` (see that folder's `agents.md`) — a single dropped spreadsheet now pauses on an explicit "Import as a list of records" vs "Secure this file as a document" step before either `onBulkDetected` or the normal single-doc `generateFingerprint`/`onFileSelect` path fires. New copy block `SPREADSHEET_MODE_LABELS` in `copy.ts` (§1.3-clean; `lint:copy` green).
## 2026-07-28 F2/F3 real client-side extraction (founder 22-LOI-format KPI, sprint amendment A3)

New `extractors/` subfolder (see its own `agents.md`) — `ocrWorker.ts` `extractText()` now genuinely extracts `.odt`/`.odp`/`.pptx`/`.epub` (F2, ZIP-XML family via new `jszip` dep) and `.rtf`/`.svg` (F3) instead of soft-failing to manual entry; also verified (no change needed) that `.md`/`.html`/`.xml`/`.json`/`.txt` already extract correctly via the existing plain-text reader. `OCRResult.method` gained `'zip-xml' | 'rtf' | 'svg'`. `.ods` is deliberately NOT handled here — coordinate with the F1 spreadsheet dual-mode (SheetJS) workstream, which owns it. `copy.ts` `OCR_LABELS.UNSUPPORTED_FILE_TYPE` updated to list the newly-supported formats. All new parsers are dynamically imported (lazy-load, same pattern as `mammoth`/`pdfjs-dist`/`tesseract.js`); `vite.config.ts` gained a `vendor-zip` manualChunk for `jszip`.
## 2026-07-28 F4 — TIFF/HEIC decode + scanned-PDF OCR fallback (founder 22-format KPI, Lane 3)

- `ocrWorker.ts` — image-family completion. TIFF (incl. multi-page, capped at `TIFF_MAX_PAGES=20`) decodes via `utif2` (MIT, pure JS); HEIC/HEIF decodes the PRIMARY image via `heic-decode` (ISC wrapper around `libheif-js`'s self-contained wasm bundle — underlying compiled `libheif` is LGPL-3.0, loaded via lazy `import()`, never statically linked — flagged for a one-time legal sanity check). Both re-encode the decoded RGBA to a real PNG via `upng-js` (MIT, pure JS, no canvas) before handing to Tesseract — this canvas-free design also made the new decode logic testable for real (not mocked) under vitest+jsdom. `extractTextFromPDF` gained a scanned/image-only PDF fallback: when the real PDF.js text layer is EMPTY across every page, pages render to a canvas and OCR via the same Tesseract path (`method: 'pdfjs-ocr'`), capped at `SCANNED_PDF_OCR_MAX_PAGES=20` pages / `SCANNED_PDF_OCR_MAX_DURATION_MS=180_000`ms wall-clock so a huge scan degrades gracefully instead of hanging the tab. A text-layer PDF NEVER takes the OCR path (checked before any render call). All Tesseract invocation was consolidated into one shared `recognizeWithTesseract()` helper (image/TIFF-page/HEIC/scanned-PDF-page all funnel through it) so the WEBEXT-02 self-host pinning and WEBEXT-03 fail-closed contract live in exactly one place. **Dominance rule preserved**: a genuinely corrupt TIFF/HEIC file (decode fails) still soft-fails as `UnsupportedImageFormatError` — SCRUM-2911's soft-fail bar moved from format-level to file-level, not removed — while a REAL Tesseract engine failure on an otherwise-valid file still fails closed as `OcrEngineLoadError`, even mid-multi-page-TIFF or mid-scanned-PDF. New ambient types `heic-decode.d.ts` / `upng-js.d.ts` (co-located here, not in `src/types/` — that folder is Supabase-generated-types-only per its own agents.md). New `vite.config.ts` `manualChunks` entries (`vendor-tiff` / `vendor-heic` / `vendor-png-encode`) keep all three decoders out of the initial bundle (lazy `import()` only, same pattern as Tesseract/PDF.js/mammoth) — measured impact: `vendor-tiff` 105.8 kB / `vendor-heic` 1,460.8 kB / `vendor-png-encode` 63.1 kB, gzipped 38.4 / 525.2 / 20.8 kB respectively, ALL absent from `dist/index.html` (confirmed no modulepreload/eager reference) and from the main `index-*.js` chunk.
- Real, small (<25 KB combined) fixture files committed under `src/lib/__fixtures__/ocr/` (png/jpg/gif/webp/tiff/multipage-tiff/22-page-overcap-tiff/heic/text-layer-pdf/scanned-pdf + truncated-corrupt variants of tiff/heic/pdf) — `ocrWorker.test.ts` decodes them for real via `utif2`/`heic-decode` (Tesseract output itself stays mocked, matching the file's existing policy); `ocrWorker.realDecode.test.ts` is a NEW, fully-unmocked companion proving `pdfjs-dist` (via the Node-safe `pdfjs-dist/legacy/build/pdf.mjs` entry — the default browser entry needs `DOMMatrix`, which jsdom doesn't implement) genuinely detects real text-layer-vs-scanned PDFs, plus real `utif2`/`heic-decode`/`upng-js` round-trips. New `ocrWorkerClientOnly.test.ts` is the §1.6 no-worker-import guard (scans `services/worker/src` for any import of `ocrWorker`/`utif2`/`heic-decode`/`libheif-js`/`upng-js`/`pdfjs-dist`/`tesseract.js`) — it also documents (does not fix) a pre-existing, unrelated finding: `services/worker/package.json` devDependencies list `pdfjs-dist`/`tesseract.js` with zero actual importers in worker source (flagged separately, not touched by this PR).
- `aiExtraction.ts` `noTextSourceKind()` — added the `'pdfjs-ocr'` OCR-method case (maps to the same `'pdf'` label as `'pdfjs'`).
## 2026-07-28 CE Registry provenance wiring (SCRUM-2913 Lane 2, PR #1618)

`sourceProvenance.ts` — `SourceProvenanceData` gained `registry_url` / `ce_envelope_sha256` (optional, nullable): the CE Registry PROVENANCE link + integrity fingerprint that `get_public_anchor` (migration `0362`) now allow-lists but that nothing on `main` previously WROTE (an inert column). The producer side lives in `services/worker/src/lib/credential-source-import.ts` (`extractCeRegistryProvenance`); the consumer side in `src/components/verification/PublicVerification.tsx` + `SourceProvenanceDisplay.tsx` (see that folder's `agents.md`). `copy.ts` — `SOURCE_PROVENANCE_LABELS` gained `REGISTRY_REFERENCE_LABEL` / `REGISTRY_REFERENCE_DESCRIPTION` (R-7 honest: provenance, never a CE-listing/endorsement claim). `copy-scrum-2938-terminology-s2.test.ts` — added `SOURCE_PROVENANCE_LABELS.REGISTRY_REFERENCE_DESCRIPTION` to `ALLOWED_PATHS` (category 4, third-party proper noun "Credential Engine", same bucket as `SHARE_LINKEDIN_DESCRIPTION`).
## 2026-07-28 Admin credit-adjust copy block (L2-A5)

`copy.ts` gained `ADMIN_CREDIT_ADJUST_LABELS` (placed right after `TREASURY_LABELS`) — the platform-admin org credit add/remove dialog on `AdminOrganizationsPage.tsx`: balance display, add/remove toggle, amount/reason inputs, the two-step review→confirm summary copy (as functions, not templated in JSX, so §1.3 `lint:copy` scans the literal string), and success/error toasts. No banned terms (§1.3) — "Credits" is not banned; internal-only page so no §1.3 exemption needed either way.
## 2026-07-28 QUEUE-01 / SCRUM-2894 (L2-A1, founder P0) — Add to Queue / Secure Instantly wiring

First real consumer of the `queueContract.ts` types (previously orphaned outside `copy.ts` — `rg queueContract` matched only copy.ts before this PR). `SecureDocumentDialog.tsx` now imports `exposedSecuringPaths`/`SecuringPath` from it; `useSecuringCapability.ts` (hooks) builds the `SecuringCapability` object. `routes.ts` gained `ROUTES.SECURE_QUEUE = '/queue'` (the `consumer_secure_queue` surface). `queryClient.ts` gained `queryKeys.secureQueue(userId, orgId, scope)`. `validators.ts` gained `validateAnchorUpdate()` (thin wrapper around the pre-existing `AnchorUpdateSchema`, used for the queue-item soft-delete). `copy.ts` gained an append-only `SECURE_QUEUE_PAGE_LABELS` block at EOF (kept separate from the existing `SECURE_QUEUE_LABELS`/`SECURING_CHOICE_LABELS` block at ~line 3420 to avoid colliding with concurrent copy.ts PRs this sprint — terminology scrub, spreadsheet dual-mode, etc.). `canSecureInstantly` is hardcoded false in `useSecuringCapability.ts` per CTO ruling R5 (Instant-Secure ships dark, flag off) — wiring the real server field is a follow-up, not this PR.
## 2026-07-28 Accept-invite route + copy + validator (SCRUM-3012)

- `routes.ts` gained `ACCEPT_INVITE: '/accept-invite'` — the previously-missing consumer of the org-invite email link (`AcceptInvitePage.tsx`).
- `copy.ts` gained `ACCEPT_INVITE_LABELS` (contiguous append-only block near `AUTH_FORM_LABELS`) — every string on the accept-invite page, including the honest "verification email could not be sent" copy so a worker-side send failure is never silently swallowed into a fake success screen.
- `validators.ts` gained `AcceptInvitationSchema` (token + 8-char-min password + optional full name) for the new-account form on that page.

## 2026-07-21 Treasury CSP + status-budget copy (SCRUM-2901, PR #1600)

- `treasuryCsp.test.ts` guards the CSP↔treasury-enrichment invariant in BOTH sources (`vercel.json` prod header + `index.html` dev/build meta): `https://mempool.space` present in `connect-src`, `'self'` regression guard, and a scoping test asserting mempool.space appears in **connect-src ONLY** (never default-src / script-src / img-src / any other directive). The R-5 drift manifest (`scripts/ci/config-drift/expected-prod-config.json` `cspConnectSrc`) is deliberately untouched — its `_note` scopes it to the cross-runtime worker+edge SUBSET, "not the full vercel.json connect-src".
- `copy.ts` `TREASURY_LABELS` gained `WORKER_STATUS_TIMED_OUT` / `WORKER_HEALTH_TIMED_OUT` (friendly copy for the 8s status-API budget firing; the raw browser TimeoutError text must never reach admins — see `useTreasuryBalance.ts`).

## 2026-07-06 S3 Lane-3 AI copy block (AI-03 — SCRUM-2383)

`copy.ts` gained `TEMPLATE_REVIEW_LABELS` in a clearly-commented contiguous block at EOF titled "S3 Lane-3 AI strings". The three S3 streams each append ONE contiguous block — do not interleave strings from other streams inside it (merge-conflict containment, mirrors the `supabase/migrations/agents.md` per-PR-block lesson).

## What This Folder Contains

Core utility modules shared across the frontend. Every write path uses Zod validation; all UI copy lives in `copy.ts`. Client-side processing modules (`piiStripper`, `fileHasher`, `aiExtraction`, `mlRuntime`, `ocrWorker`, `extractors/*`) must NEVER be imported in `services/worker/`.

## Key Files

- `supabase.ts` — typed Supabase client (anon key only, never service role)
- `routes.ts` — named route constants consumed by App.tsx and navigation
- `copy.ts` — all user-facing strings, including legal-page notices; enforces banned-term vocabulary (Constitution 1.3)
- `validators.ts` — Zod schemas for fingerprints, anchors, profiles, API keys
- `switchboard.ts` — feature flag definitions and client-side checking
- `workerClient.ts` — fetch wrapper for frontend-to-worker API calls with auth injection
- `sentry.ts` — Sentry init with mandatory PII scrubbing (Constitution 1.4). SCRUM-2249: scrubbers also collapse UUID identifiers → `[UUID]` (incl. `event.transaction` + `event.request.url`) and the Supabase project-ref → `[SUPABASE_PROJECT]`. `release` is the real build SHA (`__APP_RELEASE__`, injected in vite.config.ts from `VERCEL_GIT_COMMIT_SHA`); `server_name` tag set via `initialScope`. `IGNORED_ERROR_PATTERNS` drops benign GoTrue Navigator-lock + login AbortError noise.
- `auditLog.ts` — client-side audit event logger via POST /api/audit/event (never direct insert)
- `fileHasher.ts` — SHA-256 fingerprinting via Web Crypto (client-side only)
- `piiStripper.ts` / `enhancedPiiStripper.ts` — PII redaction before data leaves browser. `enhancedPiiStripper` is **§1.6 FAIL-CLOSED** (WEBEXT-03): when NER is requested and the model fails to load/run it THROWS (does not degrade to regex-only); regex-only is returned only on explicit `enableNER: false`.
- `nerPiiDetector.ts` — CLIENT-SIDE NER PII detection via transformers.js (`Xenova/bert-base-NER`, q8). **Self-hosted + pinned (SCRUM-2503 / #1253):** loads weights from the Arkova app origin `/models/` (`public/models/`, covered by `connect-src 'self'`) with `env.allowRemoteModels = false` — it never hits the HF CDN. Exports `NER_MODEL_ID` + `NER_MODEL_REVISION` (a pinned 40-char commit SHA, not floating `main`) so the build-time vendoring + integrity lockfile load the EXACT same artifact the runtime does, and `TRANSFORMERS_JS_VERSION` (`4.2.0`) which MUST match `scripts/ner-weights.lock.json` `transformersJsVersion` + the embedded version of the vendored bundle (skew is a hard build failure via `scripts/vendor-transformers-version.test.ts`). The runtime module is `TRANSFORMERS_BROWSER_MODULE` = `/vendor/transformers.bundle.min.js` (the self-contained build, ort inlined) and the ORT wasm pin is `ORT_WASM_VENDOR_PATH` = `/vendor/ort/` — always reference these exported constants, never a hardcoded path. (The older `public/vendor/transformers.web.min.js` was deleted by #1416; a hardcoded path to it is what silently broke the `template-review.spec.ts` intercept — see `e2e/agents.md`.) On a missing/failed load it throws a typed `NERModelLoadError` (never silently returns null). **Producer/consumer SCOPE:** #1253 is the PRODUCER (self-host loader + typed error + integrity pin); the end-to-end §1.6 fail-CLOSED guarantee — making the stripper THROW instead of degrading to regex-only — is delivered by the CONSUMER change in `enhancedPiiStripper.ts` (Lane 2 / #1262 / WEBEXT-03). Failed loads are NOT cached — a later call can retry. Weights are vendored + SHA-256-verified by `scripts/fetch-ner-model.ts` against `scripts/ner-weights.lock.json` (`public/models/` is git-ignored, the binary is never committed).
- `aiExtraction.ts` — OCR + PII strip + server extraction orchestrator (client-side). **§1.6 FAIL-CLOSED gate**: any on-device PII-model / OCR-engine failure is caught BEFORE the network call (`isPiiStripFailClosedError`), surfaces `ExtractionProgress.failClosed = true`, and returns null — NO metadata egress on the failure path.
- `ocrWorker.ts` — client-side OCR + document text-extraction dispatcher: PDF.js + Tesseract.js, `utif2`/`heic-decode`/`upng-js` (TIFF/HEIC), SheetJS (spreadsheets), and `extractors/` (ZIP-XML family + RTF + SVG). **WEBEXT-02**: Tesseract self-hosted via `TESSERACT_VENDOR_PATHS` (`/vendor/tesseract/...`, CSP `'self'`) — no `cdn.jsdelivr` runtime fetch. **WEBEXT-03**: fails closed with `OcrEngineLoadError` on any load/recognition fault. **F1 (2026-07-28)**: `.xlsx`/`.xls`/`.ods` route to `extractTextFromSpreadsheet()` (dynamic `import('xlsx')`, lazy-loaded like mammoth/pdfjs-dist) — document-mode spreadsheet anchoring, separate from row-mode bulk import. **F2/F3 (2026-07-28)**: `extractText()` also dispatches to `extractors/` — see that folder's `agents.md`. **F4 (2026-07-28)**: TIFF (multi-page)/HEIC decode to real PNG via pure-JS re-encoding (no canvas) + a scanned/image-only PDF → Tesseract fallback; see the F4 entry above.
- `ocrWorker.ts` — client-side OCR (PDF.js + Tesseract.js). **WEBEXT-02**: Tesseract self-hosted via `TESSERACT_VENDOR_PATHS` (`/vendor/tesseract/...`, CSP `'self'`) — no `cdn.jsdelivr` runtime fetch. **WEBEXT-03**: fails closed with `OcrEngineLoadError` on any load/recognition fault.
- `ocrFailClosed.ts` — **§1.6 fail-closed contract** (WEBEXT-03). Typed errors (`PiiStripFailClosedError` + `OcrEngineLoadError`/`NerPiiFailClosedError`) + `isPiiStripFailClosedError` (the single egress gate). Recognizes Lane 1's `NERModelLoadError` structurally by name (DEPENDS ON #1253 — not yet on main). **SCRUM-2911 benign (soft-fail) taxonomy** — two typed errors that are deliberately NOT `PiiStripFailClosedError` subclasses: `UnsupportedImageFormatError` (`unsupportedFormat` discriminator + `formatLabel`; HEIC/TIFF detected up-front) and `NoTextExtractedError` (`noTextExtracted` discriminator + `sourceKind: 'pdf'|'image'|'document'`; scanned image-only PDF / blank Tesseract output). Both guards (`isUnsupportedImageFormatError` / `isNoTextExtractedError`) yield to fail-closed internally — an error carrying BOTH markers is treated as FAIL-CLOSED (dominance is unit-tested). Messages/labels never carry document content. _(Restored 2026-07-28 — see `docs/incidents/2026-07-28-agents-md-union-drop-remediation.md`.)_
- `queueContract.ts` — QUEUE-01 / SCRUM-2347 frozen UX-contract TYPES for the queue-first vs. instant-secure securing experience. Pure (no React/Supabase/network). Exports the canonical `QueueLifecycleState` union (`pending`→`queued`→`processing`→`materialized`→`anchored`→`failed`/`skipped`) + `isQueueLifecycleState()` guard, the `CreditDebitState` union (`spent`/`pending`/`refunded`) mapped 1:1 to the `debit_and_enqueue` + reconciler model, `CREDIT_DEBIT_TIMING = 'on_securing'` (queueing never charges), the `SecuringPath` union (`queue`/`instant`) with `INSTANT_SECURE_DEFAULT_EXPOSED = false` (instant is HIDDEN/absent at launch; exposure gated by a server capability via `exposedSecuringPaths()`), and the three-way `QueueSurface` union (`consumer_secure_queue`/`org_duplicate_review`/`org_approvals`) that keeps the three "queue" pages from colliding. The single typed consumer for the credit-ledger + processor work. User-visible labels live in `copy.ts` (`QUEUE_LIFECYCLE_LABELS`/`CREDIT_DEBIT_LABELS`/`SECURING_CHOICE_LABELS`/`QUEUE_SURFACE_TITLES`/`SECURE_QUEUE_LABELS`); `queueContract.test.ts` asserts the 1:1 mapping is exhaustive. _(Restored 2026-07-28, lost by the union-merge-driver incident.)_
- `proofPackage.ts` — proof package generation and validation for anchor verification
- `complianceMapping.ts` — static credential-type-to-regulatory-control mapping
- `explorer.ts` — mempool.space URL builder (uses approved terminology)
- `statusDisplay.ts` — SCRUM-2003 single source of truth for human-readable anchor/attestation status labels. Pure (no React/Supabase, mirrors `formatters.ts`). `getStatusDisplay(status) → { label, tone }` / `getStatusLabel(status) → string`. Maps every `anchor_status` (PENDING/BROADCASTING→"Processing", SUBMITTED, SECURED→"Verified", REVOKED, EXPIRED, SUPERSEDED, PENDING_RESOLUTION→"Needs Review") and `attestation_status` (DRAFT, ACTIVE, CHALLENGED + shared PENDING/REVOKED/EXPIRED) value to a §1.3-compliant label with a fail-safe title-cased fallback that is also banned-term-scrubbed. The fallback scrub (`FALLBACK_BANNED_PATTERNS`) is a deliberate SUPERSET of `scripts/check-copy-terms.ts` `FORBIDDEN_TERMS` — bare-substring terms (wallet/gas/transaction/crypto/bitcoin/blockchain/mining) match without boundaries so e.g. `GASEOUS`/`WALLETED`/`TRANSACTIONAL` are scrubbed, while boundary-bounded terms (hash/block/token) keep the canonical `(?<![-\w])…(?![-\w])` boundaries so `BLOCKADE` survives — replicated locally (not imported) to keep the module browser-safe (no `node:fs`). Use this instead of rendering a raw status enum in JSX or hand-rolling another inline `statusConfig` map.
- `mlRuntime.ts` — WebGPU detection and VRAM budget for in-browser ML (2 GB cap)
- `csvExport.ts` / `csvParser.ts` / `xlsxParser.ts` — data import/export utilities
- `sourceProvenance.ts` / `badgeSvg.ts` — SCRUM-1599 public-safe source provenance helpers, evidence-level validation, badge URL construction, and fail-closed badge SVG status mapping
  - **SCRUM-2480 — the two evidence-level enums do NOT agree, and `parseVerificationLevel` is where that is reconciled.** The worker writes `captured_upload_ai` (`CREDENTIAL_EVIDENCE_VERIFICATION_LEVELS` in `services/worker/src/lib/credential-evidence.ts`); this module's `VERIFICATION_LEVEL_VALUES` says `ai_captured`. That value is persisted in `anchors.metadata.verification_level` and served by `get_public_anchor`, so before the fix every AI-captured anchor parsed as `null` and rendered **no badge at all** — which reads as "no caveat", the inverse of "this is the weakest evidence we hold" (§1.13 R-7 claims gate). The two spellings only ever met in test fixtures, so nothing caught it. Reconciliation is `VERIFICATION_LEVEL_ALIASES`, applied on READ: the server spelling is already on real rows, so renaming either enum would need a backfill, and the verification API response shape is frozen (§1.8). **If you add a level to either enum, add it to both, or add an alias here** — and never alias anything into `issuer_anchored`/`source_signed`, which are the green issuer-authenticated tiers (tested).

## Recent Changes

_The following entries were lost off `main` by the 2026-07-28 union-merge-driver incident and restored the same day — see `docs/incidents/2026-07-28-agents-md-union-drop-remediation.md`._

- 2026-07-21 SCRUM-2938 S2 (terminology scrub remainder, stacked on S1/#1609): purged user-visible "credential(s)" from `copy.ts` string values (165 occurrences scrubbed → "record(s)/document(s)" per surface). Categorized keeps: the SCRUM-1672 `ISSUE_CREDENTIAL_LABELS` carve-out (locked byte-identical), restricted-issuance adjuncts (`TOAST.CREDENTIAL_ISSUED`/`CREDENTIAL_ISSUE_FAILED`, `ORG_PAGE_LABELS.ISSUE_CREDENTIAL`, the `credential.issued` webhook description), "sign-in credentials" (different word sense), third-party proper nouns ("Credential Engine" claims-gate string, LinkedIn "Credential URL" field), and the frozen MCP tool names `verify_credential`/`search_credentials`. Contract test `copy-scrum-2938-terminology-s2.test.ts` walks EVERY exported string value and fails on any non-allowlisted hit, locks the carve-out with a full-object equality snapshot, and fails if an allowlisted path disappears. Internal keys/export names (`CREDENTIAL_TYPE_LABELS`, `MY_CREDENTIALS_LABELS`, …) intentionally unchanged (§1.3 internal-code exemption).
- 2026-07-21 SCRUM-2911 (scanned-PDF no-text soft-fail, PR #1605): `ocrFailClosed.ts` gained `NoTextExtractedError` + `isNoTextExtractedError` (benign, NOT fail-closed; `noTextExtracted` discriminator; `sourceKind` label; fail-closed DOMINATES in the guard — mirrors `UnsupportedImageFormatError` exactly). `aiExtraction.ts` replaced the hardcoded 'No text found in document. Try a clearer scan.' inline early-return (§1.3 violation, stringly-typed) with the typed error thrown at the empty/whitespace-text check and routed through the same catch chain as unsupported-format → soft error (`stage:'error'`, NO `failClosed`, fixed copy — never `err.message` — so duck-typed cross-bundle errors can't smuggle text into the UI). `copy.ts` gained `AI_EXTRACTION_LABELS.NO_TEXT_FOUND`. Regression matrix unit-tested: scanned-pdf/blank-image → soft (`extraction-failed` step), OCR-engine + NER-model failure → still fail-closed (`privacy-blocked`), both-markers overlap → fail-closed, zero fetch on every no-text path. SecureDocumentDialog needed NO change (existing else-branch routes soft failures).
- 2026-07-08 FE-PROOF-GATE review fix (SCRUM-2501): `proofAvailability.ts` now prefers the worker's additive `proof_error_code` (`NO_BATCH_PROOF` / `RECORD_NOT_FOUND`) on `/proof` 404 responses and falls back only to the exact legacy prose strings. Unknown 404 bodies route to the retry state instead of the honest direct-anchored empty state, so contract drift is not silently presented as "no downloadable proof." `PROOF_AVAILABILITY_LABELS.NOT_YET_AVAILABLE_BODY` avoids referencing a Network Receipt because direct-anchored records may not have one.
- 2026-07-06 WEBEXT-01-FIX F-1/F-2/F-3 (Lane 1 S3, gate #13 §1.6): made in-browser NER inference ACTUALLY EXECUTE under the deployed CSP. **F-1 (P1, was live in prod):** `nerPiiDetector.ts` now imports `/vendor/transformers.bundle.min.js` — the SELF-CONTAINED transformers.js 4.2.0 browser build (`dist/transformers.min.js`, ort inlined) — replacing the `.web.` build whose top-level bare specifiers (`onnxruntime-web/webgpu`, `onnxruntime-common`) made native browser module-linking throw `TypeError: Failed to resolve module specifier` on EVERY load (weights/CSP irrelevant; NER dead-on-arrival, PR #1409 §6). `TRANSFORMERS_BROWSER_MODULE` is now exported so locks/gates pin the exact path. **F-2 (latent):** new exported `ORT_WASM_VENDOR_PATH = '/vendor/ort/'`; the loader pins `env.backends.onnx.wasm.wasmPaths` to it BEFORE `pipeline(...)` (session creation) — unset, onnxruntime-web defaults to a jsdelivr CDN URL the deployed CSP (`connect-src 'self'`) blocks. If the runtime exposes no `backends.onnx.wasm` to pin, the loader throws typed `NERModelLoadError` BEFORE session creation (fail-closed, pipeline never invoked). Artifacts vendored + hash-locked by `scripts/vendor-ner-runtime.ts` / `scripts/ner-runtime.lock.json` (prebuild); `scripts/ci/check-csp-runtime-deps.ts` now fails the build on bare/off-origin specifiers in vendored runtime ESM and on a missing/off-origin wasmPaths pin. Loader contract UNCHANGED: `allowRemoteModels=false`, `localModelPath='/models/'`, pinned model revision, typed fail-closed, regex-only only on explicit `enableNER:false`. **F-3 (found by this fix's browser-real probe):** transformers.js 4.2.0's `token-classification` output carries NO char offsets — the old `mergeEntities` passed `undefined` spans through and `redactNEREntities` sliced with them, duplicating the text 2^n× with ALL PII STILL PRESENT (invisible on main only because F-1 blocked NER first); `mergeEntities` now computes spans against the input text (cursor-based exact match + token-walk locate; provided offsets honored when valid) and `redactNEREntities` adds a literal-text fallback and THROWS typed fail-closed (no PII in the message) when a detected entity cannot be located.
- 2026-07-06 SCRUM-2495 (ABUSE-DISCLAIMER, Lane 2 T1): `copy.ts` — added `DOES_NOT_ASSERT_LABELS` (TITLE, MEASURED_LABEL/BODY, ASSERTED_LABEL/BODY, NOT_ASSERTED_LABEL/BODY) per CLAUDE.md §1.5. Consumed by the new `src/components/verification/DoesNotAssertDisclaimer.tsx`, which replaces a prior JSX-inline disclaimer paragraph in `PublicVerification.tsx` that contained a live §1.3 banned-term violation ("Bitcoin blockchain") invisible to `lint:copy`'s line-based scanner (see `src/components/verification/agents.md`). §1.3-clean; `lint:copy` green.
- 2026-07-17 SCRUM-2910 (BUG-2026-07-17-009/-010, P0): `fraudDetection.ts` gained `isFraudMetadataKey(key)` — normalized-prefix predicate hiding all fraud-derived metadata keys (`fraud_score`, `fraud_signals`, camelCase `fraudSignals`, ...) from every display surface. Detection/write path unchanged.
- 2026-07-06 AI-03 round-1 review (PR #1413): `aiExtraction.ts` — documented at the stamping site that per-field confidence is NOT real: `/ai/extract` returns ONE overall confidence stamped onto every field, so review gating is overall-confidence-driven until per-field confidence exists (follow-up story). `copy.ts` — removed dead `TEMPLATE_REVIEW_LABELS.HIGH_CONFIDENCE_BADGE` (never rendered).
- 2026-07-06 UX-03 follow-up — copy-lint now enforces `copy.ts` (SCRUM-1029 / SCRUM-2149 lineage): removed `src/lib/copy.ts` from `scripts/check-copy-terms.ts` `EXCLUDE_PATTERNS` so the largest user-facing copy surface is finally scanned. Sanctioned SCRUM-1672 "Issue Credential" strings pass via a new `scripts/ci/snapshots/copy-terms-allowlist.json` (permanent §1.3 carve-out, `(file,term)`-keyed/drift-immune, fail-closed to `ALLOWLISTABLE_TERMS`={`issue credential`}); the 4 COMP-03 `INDEPENDENT_VERIFY_LABELS` §1.3 hits reworded at source (SHA-256 hash→fingerprint; block explorer→network explorer; `{hash}`→`{fingerprint}`). New shared `isNonSuppressibleTerm` guard blocks BOTH the allowlist and the grandfather baseline from ever silencing a secret (`service_role`/`postgrest`), the `worker service` infra leak, or a launch-blocker (closes a pre-existing category-blind baseline hole). `lint:copy` green (357 files, +copy.ts). Scanner unit tests +13 (allowlist term/file-scoping, fail-closed loader, non-suppressible guard, e2e-0-fresh on real copy.ts, new-term-in-copy.ts fails). Closes the "Known gap" note below.
- 2026-07-06 UX-03 copy fix (SCRUM-1029): `copy.ts` — `API_KEY_LABELS.USAGE_UNAVAILABLE` reworded "…worker service not connected" → "…service not connected" to drop the banned "worker service" engineering term (rendered by `ApiUsageDashboard`'s error state, surfaced by `ApiKeySettingsPage`). `lint:copy` never caught it because `copy.ts` is in `EXCLUDE_PATTERNS` — see the **Known gap** note under Copy-lint coverage. Updated the two literal assertions in `ApiUsageDashboard.test.tsx` + the `e2e/api-keys.spec.ts` offline-state matcher; `ApiKeySettingsPage.test.tsx`'s `/worker service/i → null` guard now passes for the right reason. TDD (tests red → green, 18/18); typecheck + eslint + `lint:copy` green; §1.3-clean.
- 2026-07-01 DRIVE-01 (SCRUM-2366, Lane 3 S2): `copy.ts` — added Google Drive connect entitlement copy to `CONNECTIONS_LABELS` (`GOOGLE_DRIVE_NAME`/`_DESC`, `DRIVE_NOT_ADMIN`, `DRIVE_ORG_NOT_VERIFIED`, `DRIVE_ORG_SUSPENDED`, `DRIVE_NEEDS_PAID_PLAN`, `DRIVE_INDIVIDUAL_NOT_VERIFIED`, `DRIVE_GATE_CHECKING`, `DRIVE_GATE_UNAVAILABLE`). Each string mirrors a worker `DriveConnectDenyReason` from `drive-connect-eligibility.ts` so the UI message and the backend verified-only gate stay in lockstep. §1.3-clean (`lint:copy` green).
- 2026-06-29 PROOF-04 (SCRUM-2337, Lane 1 S2): the audit certificate now embeds a machine-readable **proof packet** so it can be re-verified offline. `generateAuditReport.ts` split into pure `buildAuditReport(data)` (returns `{ doc, filename, embeddedProofJson }`, no DOM/save) + the `generateAuditReport(data)` download wrapper; new `buildProofPacket(data)` returns a strict allow-list `ProofPacket` (fingerprint, merkle root/proof/index, tx/block fields, op_return payload, schema version, observed time, optional signature metadata) — gated SECURED-only via `isProofDownloadable` and `null` otherwise. §1.6: client-side only; the packet NEVER carries document bytes, filename, issuer, file size, or any record id (unit-tested). The packet is embedded in PDF doc-properties (`keywords`, subject `arkova-proof-packet`) AND rendered visibly, with a "Verify This Certificate Offline" block pointing at the reference verifier. All certificate strings moved into `copy.ts` `CERTIFICATE_COPY` (§1.3-clean; `lint:copy` green); status label comes from `getStatusDisplay` (no hardcoded "Verified"). New `isProofDownloadable(status)` in `statusDisplay.ts` (SECURED-only, fails closed) is the single proof-download gate consumed by Lane 2 per `docs/lane1/fe-proof-gate-contract-s2.md`. Tests: `generateAuditReport.test.ts` (9) + `isProofDownloadable` cases in `statusDisplay.test.ts`.
- 2026-06-29 PROOF-04 rework (Carson P1 review on #1352): `ProofPacket` is now the **canonical `proof_bundle`** field-for-field with PROOF-05 (`ProofBundle` in `services/worker/src/api/v1/verify-proof.ts`) and the PROOF-07 CLI (`packages/verifier-cli/src/types.ts`). `merkle_proof` is `MerkleProofEntry[]` (`{ hash, position }`) — **never** flattened to `string[]` (that dropped the side, breaking offline root recompute); `observed_time` → `block_timestamp` (machine field; PDF label still "Network Observed Time"); `proof_schema_version` non-null, defaults to `1`; added `merkle_index` + `leaf_count` (`leaf_count` has no column today → `null`); `signature` is `{ alg, signing_key_id } | null`. `buildProofPacket` validates each branch entry via `isMerkleProofEntry` and preserves it whole. Contract `docs/lane1/fe-proof-gate-contract-s2.md` aligned (byte-identical claim now true); Lane 2 (SCRUM-2501) builds against this corrected shape.
- 2026-06-25 iter-5 webhook-toggle: `copy.ts` — added `WEBHOOK_LABELS.TOGGLE_ERROR` ("Couldn't update this endpoint. You may not have permission. Please try again."), surfaced by `WebhookSettingsPage.handleToggle` when an enable/disable update is denied (RLS/permission). Generic, user-facing copy — the raw Supabase/Postgres error is never shown. §1.3-clean (no banned terms; `lint:copy` green).
- 2026-06-24 WEBEXT-CSP / SCRUM-2503 / #1253 (defect fix): **(1) transformers.js version skew** — the vendored runtime bundle `public/vendor/transformers.web.min.js` was the 4.1.0 build while the integrity lock, `package.json` pin, and tests all assumed 4.2.0 (a mismatch that could break runtime model loading while CI stayed green). Re-vendored the bundle to the verified 4.2.0 web/ESM build (byte-for-byte from `node_modules/@huggingface/transformers@4.2.0/dist/transformers.web.min.js`, Apache-2.0); updated `public/vendor/agents.md` to 4.2.0 + source + license. Added `TRANSFORMERS_JS_VERSION` const to `nerPiiDetector.ts` + a runtime version-skew guard (throws `NERModelLoadError` if `env.version` ≠ pinned). New regression test `scripts/vendor-transformers-version.test.ts` (4) fails the build if the vendored bundle's embedded version ≠ `ner-weights.lock.json.transformersJsVersion` ≠ `TRANSFORMERS_JS_VERSION`, so the skew can never recur silently. The lock file set was already correct for 4.2.0 (q8 → `onnx/model_quantized.onnx`; existing `fetch-ner-model.test.ts` asserts it). **(2) claims honesty (§1.13 R-7)** — scoped #1253's docs (module header + `NERModelLoadError` doc in `nerPiiDetector.ts`, the lockfile `_comment`, and this file) to state plainly that #1253 is the PRODUCER half (self-host loader + typed error + integrity pin) and that the end-to-end §1.6 fail-CLOSED guarantee requires the CONSUMER change in #1262 / WEBEXT-03 (Lane 2 — `enhancedPiiStripper.ts` THROWs instead of regex-degrading). Did NOT touch `enhancedPiiStripper.ts` (Lane 2 surface).
- 2026-06-24 S1.4b / WEBEXT-CSP (SCRUM-2503): pinned + integrity-verified the self-hosted NER model so it ships in the deploy build. `nerPiiDetector.ts` now exports `NER_MODEL_ID` + `NER_MODEL_REVISION` (`24c7e5aba9ae350923357a6f0b92571be34037ec` — the model's main commit SHA, NOT floating `main`); the loader passes `revision` to `pipeline(...)`. New committed lockfile `scripts/ner-weights.lock.json` maps each requested file → real SHA-256 → bytes for the EXACT file set transformers.js v4.2.0 requests for this model in q8 (`config.json`, `tokenizer.json`, `tokenizer_config.json`, `onnx/model_quantized.onnx` as required; `special_tokens_map.json` verified-if-present). `scripts/fetch-ner-model.ts` rewritten to fetch from the pinned revision, verify SHA-256 + byte length against the lock, and EXIT NON-ZERO on any mismatch/missing required file (build fails closed); idempotent (skips files already present + matching). Build wiring: root `package.json` gained a `prebuild` script (`tsx scripts/fetch-ner-model.ts`) so Vercel populates `public/models/` before `vite build` — weights stay git-ignored, fetched at build, served same-origin under CSP `'self'` (runtime never touches the HF CDN). New tests `scripts/fetch-ner-model.test.ts` (11) cover the hash gate (mismatch/length → fail, match → pass), pinned-revision enforcement (rejects `main`), and lockfile↔detector-constant sync; the existing 16 `nerPiiDetector.test.ts` stay green (27 total). Follow-up: weights are HF-fetched at build for now — later they should be mirrored into Arkova storage (e.g. an Arkova bucket / the edge worker origin) so the build doesn't depend on HF availability/rate-limits.
- 2026-06-23 SCRUM-2347 (QUEUE-01, Lane-2, T0): added `queueContract.ts` (frozen queue/instant-secure UX-contract types) + its `*.test.ts`, and the `QUEUE_LIFECYCLE_LABELS` / `QUEUE_LIFECYCLE_DESCRIPTIONS` / `CREDIT_DEBIT_LABELS` / `CREDIT_DEBIT_DESCRIPTIONS` / `SECURING_CHOICE_LABELS` / `SECURING_CHOICE_HINTS` / `QUEUE_SURFACE_TITLES` / `SECURE_QUEUE_LABELS` blocks in `copy.ts`. Spec doc: `docs/sprint-0/lane2/queue-instant-ux-contract.md`. No worker/schema/component change. Launch posture frozen: **queue-first is the only exposed securing path; "Secure Instantly" is absent (not greyed) until the worker grants the capability** (read from `/api/billing/status`, never a client default). Charge happens at securing, never at queueing. Retires the hardcoded `<h1>Review queue</h1>` collision (`AnchorQueuePage.tsx:322`) by routing the three distinct queue surfaces through distinct copy titles. _(Restored 2026-07-28, lost by the union-merge-driver incident.)_
- 2026-06-23 WEBEXT-02/03/04 (SCRUM-2504/2505/2506) — §1.6 fail-closed restore (the 2026-06-16 fail-open regression fix). New `ocrFailClosed.ts` (fail-closed error contract + egress gate). `ocrWorker.ts` self-hosts Tesseract under CSP `'self'` (`TESSERACT_VENDOR_PATHS` → `public/vendor/tesseract/`) and fails closed on OCR-engine load/run failure. `enhancedPiiStripper.ts` now THROWS on NER failure instead of silently degrading to regex-only. `aiExtraction.ts` catches any fail-closed error BEFORE the `/api/v1/ai/extract` fetch and surfaces `failClosed` — proven by the unit suites + `e2e/extraction-csp-fail-closed.spec.ts`. Co-merges with #1253 (Lane 1's `NERModelLoadError` self-host), matched structurally by name.
- 2026-06-22 S1.4 / WEBEXT-CSP (SCRUM-2503): `nerPiiDetector.ts` now SELF-HOSTS the NER model. The loader sets `env.allowRemoteModels = false` + `env.localModelPath = '/models/'` (exported as `NER_LOCAL_MODEL_PATH`) before building the pipeline, so on-device PII stripping loads weights from the Arkova origin and the prod CSP no longer triggers a silent HF-CDN fetch → regex-only fallback (§1.6 fail-OPEN). Added `NERModelLoadError` (typed, carries `cause`) thrown when the self-hosted model can't load — the loader never returns a falsy pipeline. A rejected load is no longer cached (clears the singletons so a retry can succeed). New ops script `scripts/fetch-ner-model.ts` downloads the q8 weights+tokenizer into `public/models/Xenova/bert-base-NER/`; that path is git-ignored (binary never committed) — run it in local dev + the deploy pipeline before `vite build`. No `vercel.json`/CSP edit needed: `/models/` is same-origin, already covered by `connect-src 'self'`. Did NOT touch `enhancedPiiStripper.ts` (Lane 2 / WEBEXT-03 catches `NERModelLoadError` to fail closed) or `piiStripper.ts`.
- 2026-06-05 SCRUM-2246 (HARDEN-1-C): `lazyWithRetry.ts` — resilient `React.lazy` wrapper for route code-splitting. `loadWithRetry()` retries a `() => import()` loader N times (default 2, linear backoff) and, on a persistent CHUNK-LOAD error only, sets a `sessionStorage` sentinel (`arkova:chunk-reload`) and calls `location.reload()` ONCE to pick up a fresh `index.html` after a deploy renamed content-hashed chunks. If the sentinel is already set (reload already happened, chunk still missing) it rethrows so the route error boundary renders a graceful fallback instead of looping. Non-chunk errors fail fast and never reload. `isChunkLoadError()` matches a SET of Chrome/Firefox/Safari/Vite/webpack chunk-failure signatures, not one string. Sentinel clears on any successful load. `copy.ts` gained `ERROR_BOUNDARY_LABELS.STALE_VERSION_*`. Fixes Sentry FRONTEND-3/8 ("Failed to fetch dynamically imported module").
- 2026-05-26 SCRUM-2013: `validators.ts` and `csvParser.ts` credential type lists expanded to 27 canonical values, adding `CPE`, `ACCREDITATION`, `CONTRACT_PRESIGNING`, and `CONTRACT_POSTSIGNING`.
- 2026-05-29 SCRUM-1958 (subtask-4): `switchboard.ts` — flipped the **code default** of `ENABLE_SEMANTIC_SEARCH` to `false` so non-prod (local dev / preview) hides smart search until the `credential_embeddings` backfill lands. Production is driven by the `switchboard_flags` row, not this default (DB row untouched in this change). `copy.ts` — added `SEMANTIC_SEARCH_LABELS` (heading, placeholder, friendly match-strength labels, honest empty state, and 402/503/network/generic error copy). No client audit call for the search query — the worker records AI usage server-side (§1.6).

## Do / Don't Rules

- DO: Validate with Zod before any Supabase write
- DO: Put all UI-visible strings in `copy.ts`, not inline JSX
- DON'T: Import `piiStripper`, `fileHasher`, `aiExtraction`, `mlRuntime`, or `ocrWorker` in `services/worker/`
- DON'T: Expose service role key, raw API keys, or user emails in any module
- DON'T: Cast `verification_level` strings directly; use `parseVerificationLevel()` so unknown values disappear instead of rendering misleading evidence labels

## Copy-lint coverage (SCRUM-2149)
`src/lib/**` is now scanned by `npm run lint:copy` (`scripts/check-copy-terms.ts`) for banned §1.3 terms in **user-visible strings** — JSX text and quoted display/error copy that reaches users (e.g. proof-package glossary text, Zod validation messages). The linter does NOT flag code positions (type unions, object keys, property access, URL segments, bare in-code enum/config values like `'mainnet'`), so internal chain/network identifiers in `explorer.ts`/`env.ts` are fine; only display strings must use approved vocabulary. **`copy.ts` is now scanned too** (UX-03 follow-up, 2026-07-06 — it is shipped copy, not just rule docs); its sanctioned SCRUM-1672 "Issue Credential" strings pass via `scripts/ci/snapshots/copy-terms-allowlist.json`. 2026-05-30: reworded `proofPackage.ts` proof_glossary ("SHA-256 hash" → "SHA-256 fingerprint", §1.3 Hash→Fingerprint).

**Known gap (2026-07-06) — CLOSED (UX-03 follow-up):** the blanket `copy.ts` exclusion was a blind spot — most strings in `copy.ts` are shipped user-facing copy, so a banned term there reached users while `lint:copy` stayed green. This let "worker service" ship in `USAGE_UNAVAILABLE` (fixed, UX-03/SCRUM-1029). **Fix shipped:** `copy.ts` is removed from `EXCLUDE_PATTERNS` and now scanned; the 4 sanctioned SCRUM-1672 "Issue Credential" hits (`CREDENTIAL_ISSUE_FAILED`, `ISSUE_CREDENTIAL_LABELS.GATE_*`) pass via a dedicated `scripts/ci/snapshots/copy-terms-allowlist.json` (permanent §1.3 carve-out, `(file, term)`-keyed → drift-immune, fail-closed to the tiny `ALLOWLISTABLE_TERMS` set), and the 4 COMP-03 `INDEPENDENT_VERIFY_LABELS` gray-area hits were **reworded** at source ("SHA-256 hash" → "SHA-256 fingerprint"; "public block explorer" → "public network explorer"; `{hash}` token → `{fingerprint}`). A shared `isNonSuppressibleTerm` guard means neither the allowlist nor the baseline can ever silence a secret leak (`service_role`/`postgrest`), the infra leak `worker service`, or a launch-blocker. `shouldCheck('src/lib/copy.ts')` now returns `true`. See `scripts/agents.md` → "Copy-term sanctioned allowlist".
- 2026-07-28 L3-A6: `copy.ts` gained `CE_REGISTRY_IMPORT_LABELS` (new section, "PUBLIC REGISTRY IMPORT") + `MY_CREDENTIALS_LABELS.ADD_FROM_REGISTRY` — append-only, no existing keys touched. §1.3-safe: "Fingerprint" not "Hash", no chain terminology, "Add Record" (not "Issue Credential" — SCRUM-1672 restricts that phrase to the verified-org issuance flow, which this is not). Backs `CtdlRegistryImportDialog.tsx` (`src/components/credentials/`), the CE Noncredit Data Taxonomy 3.0 anchoring POC UI entry point.
- 2026-06-29 PROOF-04 second-pass (Carson P1 #1352): new `src/lib/sourceProofInput.ts` — `sourceProofInput(supabase, anchor)` assembles the embedded `ProofInput` AND **sources `leaf_count`**, the field that arms the CVE-2012-2459 guard. The old download path never set it (anchor_proofs has no `leaf_count` column), so every embedded packet shipped `leaf_count: null` and the cert couldn't run the full offline guard for any **batch** proof. Now derived the way the server does (PROOF-05): an RLS-scoped exact head-count over `anchor_proofs` filtered by the indexed `batch_id` — a number, no rows/PII (§1.6). That exact-head-count call site is registered against the R0-8 baseline (`check-count-exact-baseline.ts`) via the sanctioned `count-exact-allowed` PR label: the count is bounded per-batch over an indexed column, not a scan of the hot `anchors` table the guardrail targets, and it mirrors the server (#1354). Single-leaf / un-batched SECURED rows → `leaf_count = 1`. If a batch member's count can't be sourced, the result is `{ proof, complete:false }` — the page passes `proofComplete:false` to `buildAuditReport`, which swaps the offline-verify intro to `OFFLINE_VERIFY_INTRO_INCOMPLETE` (drops the "complete proof" claim, §1.5) and warns the user; the packet still embeds for inspection. Tests: `sourceProofInput.test.ts` (6) + 2 incomplete-cert cases in `generateAuditReport.test.ts`.
