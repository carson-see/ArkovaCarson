# agents.md — types
_Last updated: 2026-08-10_

## 2026-08-10 — organization_field_policies (manual patch, not a live regen)

Migration `0405` (file-only, unapplied) adds `public.organization_field_policies`. Hand-patched into
`Tables` in BOTH copies (`src/types/database.types.ts` and
`services/worker/src/types/database.types.ts`) in alphabetical position, with the `org_id` FK
Relationships entries the codegen emits for an `organizations` reference. Done by hand for the same
reason the `0400` row in `supabase/migrations/agents.md` gives: the shared local Supabase stack is
concurrently mutated by other worktree sessions, so no schema-changing apply was made against it.
**Regenerate for real** (`npm run gen:types`) once `0405` lands on a reachable project — note that
`folders` (migration `0365`, merged) is still missing from these files, so a regen will correct more
than this table.

## 2026-07-28 R19 — anchors.fingerprint_source (manual patch, not a live regen)

Migration `0376` (unmerged, file-only) adds `anchors.fingerprint_source text` (nullable, CHECK-constrained). Hand-patched into the `anchors` Row/Insert/Update shapes here since the migration hasn't been applied to any project this session could `gen:types` against. **Regenerate for real** (`npx supabase gen types typescript`) once `0376` lands on a reachable Supabase project — this manual patch is a stopgap, not a substitute.

## What This Folder Contains

Auto-generated TypeScript type definitions for the Supabase database schema.

## Key Files
- `database.types.ts` — generated by `supabase gen types typescript`; exports `Database`, `Json`, and per-table `Row` / `Insert` / `Update` types used by every Supabase query in the app

## Recent Updates
- 2026-07-28 (L2-A5): hand-added the `admin_adjust_org_credit` RPC entry (migration `0375`, file-only/pre-soak — not yet on the local dev DB's applied ledger, so a full `gen:types` regen wasn't run). Matches the shape the generator would emit, mirrored byte-identically in `services/worker/src/types/database.types.ts`. Follow-up: re-run `npm run gen:types` once 0375 lands on the local ledger to confirm.
- 2026-07-15: regenerated from a clean local ledger through migration 0358. Adds `anchor_txid_journal` + `resolve_anchor_txid_journal`; frontend and worker outputs are byte-identical. No linked environment was used.
- 2026-06-16: full resync via `npm run gen:types` from a clean `supabase db reset --local` (head 0339). The file had drifted since the 0326 regen below. Purely additive catch-up — new tables `external_document_versions` + `version_reviews` (0323), column `webhook_dead_letter_queue.failure_kind` (0338), and RPCs `admin_set_org_anchor_quota`, `bump_cloud_logging_retry_counts` (0322), `get_public_anchor_by_fingerprint` (0339). No removals (one `org_integrations` relationship reorder + the previously-missing `inherited_from_org_id_fkey → public_org_profiles` entry). Frontend + worker `tsc --noEmit` both green.
- SCRUM-1649: regenerated after migration 0326 added `org_credit_deductions` and updated org-credit RPC return metadata.

## Do / Don't Rules
- DO: Regenerate with `npx supabase gen types typescript` after every migration
- DON'T: Hand-edit this file — it will be overwritten on next generation
- DON'T: Add non-generated types here — put custom types in the relevant module or a dedicated `.d.ts`
